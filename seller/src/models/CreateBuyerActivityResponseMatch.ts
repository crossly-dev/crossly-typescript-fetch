/* tslint:disable */
/* eslint-disable */
/**
 * Crossly Seller API
 * The seller-facing Crossly API — listings, crossposting, orders, analytics. Authenticate with a Personal Access Token (`Authorization: Bearer crossly_pat_…`) or a seller OAuth token (`crossly_oat_…`), scoped `<domain>:<read|write>`. Buyer endpoints are a SEPARATE API with a separate principal — see the Crossly Buyer API.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { exists, mapValues } from '../runtime';
import {
    CreateBuyerActivityResponseMatchOffer,
    CreateBuyerActivityResponseMatchOfferFromJSON,
    CreateBuyerActivityResponseMatchOfferFromJSONTyped,
    CreateBuyerActivityResponseMatchOfferToJSON,
} from './CreateBuyerActivityResponseMatchOffer';
import {
    GetCatalogLookupResponseCatalog,
    GetCatalogLookupResponseCatalogFromJSON,
    GetCatalogLookupResponseCatalogFromJSONTyped,
    GetCatalogLookupResponseCatalogToJSON,
} from './GetCatalogLookupResponseCatalog';

/**
 * 
 * @export
 * @interface CreateBuyerActivityResponseMatch
 */
export interface CreateBuyerActivityResponseMatch {
    /**
     * 
     * @type {string}
     * @memberof CreateBuyerActivityResponseMatch
     */
    verdict: CreateBuyerActivityResponseMatchVerdictEnum;
    /**
     * 
     * @type {CreateBuyerActivityResponseMatchOffer}
     * @memberof CreateBuyerActivityResponseMatch
     */
    offer: CreateBuyerActivityResponseMatchOffer;
    /**
     * Positive when cheaper. Null when no page price was supplied.
     * @type {number}
     * @memberof CreateBuyerActivityResponseMatch
     */
    savingCents?: number | null;
    /**
     * TRUE means the offer's condition and the page's are comparable. False
means we matched the item but not its state — a used Crossly copy against
a retailer's new one — and the UI must say so rather than claim a saving.
     * @type {boolean}
     * @memberof CreateBuyerActivityResponseMatch
     */
    conditionComparable: boolean;
    /**
     * Always false today. Crossly shipping is computed at checkout from the
buyer's address, which Scout does not have and should not send. Present so
the surface that renders "before shipping" is reading a fact rather than
hard-coding an assumption that stops being true when we add it.
     * @type {boolean}
     * @memberof CreateBuyerActivityResponseMatch
     */
    shippingKnown: boolean;
    /**
     * 
     * @type {GetCatalogLookupResponseCatalog}
     * @memberof CreateBuyerActivityResponseMatch
     */
    catalog?: GetCatalogLookupResponseCatalog | null;
    /**
     * Other buyable offers for the SAME item, cheapest first, best excluded.

Deliberately not "you might also like". We have no behavioural data to
build that from, and inventing it would put unrelated items under a badge
whose entire value is that it only ever appears when we have the thing the
buyer is actually looking at.

What these ARE is the same product from other sellers, in other
conditions, at other prices — which is the choice a buyer on a product
page genuinely wants, and the one a single "cheapest" result hides.
     * @type {Array<CreateBuyerActivityResponseMatchOffer>}
     * @memberof CreateBuyerActivityResponseMatch
     */
    alternates: Array<CreateBuyerActivityResponseMatchOffer>;
}

/**
* @export
* @enum {string}
*/
export enum CreateBuyerActivityResponseMatchVerdictEnum {
    Cheaper = 'cheaper',
    NotCheaper = 'not_cheaper',
    NoPagePrice = 'no_page_price',
    CurrencyMismatch = 'currency_mismatch'
}

export function CreateBuyerActivityResponseMatchFromJSON(json: any): CreateBuyerActivityResponseMatch {
    return CreateBuyerActivityResponseMatchFromJSONTyped(json, false);
}

export function CreateBuyerActivityResponseMatchFromJSONTyped(json: any, ignoreDiscriminator: boolean): CreateBuyerActivityResponseMatch {
    if ((json === undefined) || (json === null)) {
        return json;
    }
    return {
        
        'verdict': json['verdict'],
        'offer': CreateBuyerActivityResponseMatchOfferFromJSON(json['offer']),
        'savingCents': !exists(json, 'savingCents') ? undefined : json['savingCents'],
        'conditionComparable': json['conditionComparable'],
        'shippingKnown': json['shippingKnown'],
        'catalog': !exists(json, 'catalog') ? undefined : GetCatalogLookupResponseCatalogFromJSON(json['catalog']),
        'alternates': ((json['alternates'] as Array<any>).map(CreateBuyerActivityResponseMatchOfferFromJSON)),
    };
}

export function CreateBuyerActivityResponseMatchToJSON(value?: CreateBuyerActivityResponseMatch | null): any {
    if (value === undefined) {
        return undefined;
    }
    if (value === null) {
        return null;
    }
    return {
        
        'verdict': value.verdict,
        'offer': CreateBuyerActivityResponseMatchOfferToJSON(value.offer),
        'savingCents': value.savingCents,
        'conditionComparable': value.conditionComparable,
        'shippingKnown': value.shippingKnown,
        'catalog': GetCatalogLookupResponseCatalogToJSON(value.catalog),
        'alternates': ((value.alternates as Array<any>).map(CreateBuyerActivityResponseMatchOfferToJSON)),
    };
}

