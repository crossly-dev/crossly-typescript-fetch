/* tslint:disable */
/* eslint-disable */
/**
 * Crossly Seller API
 * The seller-facing Crossly API — listings, crossposting, orders, analytics. Authenticate with a Personal Access Token (`Authorization: Bearer crossly_pat_…`) or a seller OAuth token (`crossly_oat_…`), scoped `<domain>:<read|write>`. Buyer endpoints are a SEPARATE API with a separate principal — see the Crossly Buyer API.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { exists, mapValues } from '../runtime';
import {
    CreateMagicScanSynthesizeResponsePayloadCategory,
    CreateMagicScanSynthesizeResponsePayloadCategoryFromJSON,
    CreateMagicScanSynthesizeResponsePayloadCategoryFromJSONTyped,
    CreateMagicScanSynthesizeResponsePayloadCategoryToJSON,
} from './CreateMagicScanSynthesizeResponsePayloadCategory';
import {
    CreateMagicScanSynthesizeResponsePayloadChosenDimensions,
    CreateMagicScanSynthesizeResponsePayloadChosenDimensionsFromJSON,
    CreateMagicScanSynthesizeResponsePayloadChosenDimensionsFromJSONTyped,
    CreateMagicScanSynthesizeResponsePayloadChosenDimensionsToJSON,
} from './CreateMagicScanSynthesizeResponsePayloadChosenDimensions';
import {
    CreateMagicScanSynthesizeResponsePayloadDimensions,
    CreateMagicScanSynthesizeResponsePayloadDimensionsFromJSON,
    CreateMagicScanSynthesizeResponsePayloadDimensionsFromJSONTyped,
    CreateMagicScanSynthesizeResponsePayloadDimensionsToJSON,
} from './CreateMagicScanSynthesizeResponsePayloadDimensions';
import {
    CreateMagicScanSynthesizeResponsePayloadGrading,
    CreateMagicScanSynthesizeResponsePayloadGradingFromJSON,
    CreateMagicScanSynthesizeResponsePayloadGradingFromJSONTyped,
    CreateMagicScanSynthesizeResponsePayloadGradingToJSON,
} from './CreateMagicScanSynthesizeResponsePayloadGrading';
import {
    CreateMagicScanSynthesizeResponsePayloadSectionApplicability,
    CreateMagicScanSynthesizeResponsePayloadSectionApplicabilityFromJSON,
    CreateMagicScanSynthesizeResponsePayloadSectionApplicabilityFromJSONTyped,
    CreateMagicScanSynthesizeResponsePayloadSectionApplicabilityToJSON,
} from './CreateMagicScanSynthesizeResponsePayloadSectionApplicability';

/**
 * 
 * @export
 * @interface CreateMagicScanSynthesizeResponsePayload
 */
export interface CreateMagicScanSynthesizeResponsePayload {
    /**
     * 
     * @type {string}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    title: string;
    /**
     * 
     * @type {string}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    description: string;
    /**
     * 
     * @type {string}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    brand?: string | null;
    /**
     * 
     * @type {CreateMagicScanSynthesizeResponsePayloadGrading}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    grading?: CreateMagicScanSynthesizeResponsePayloadGrading | null;
    /**
     * 
     * @type {string}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    color?: string | null;
    /**
     * 
     * @type {string}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    material?: string | null;
    /**
     * 
     * @type {string}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    size?: string | null;
    /**
     * Poshmark/Vestiaire size system — "US", "EU", "UK", etc.
     * @type {string}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    sizeSystem?: string | null;
    /**
     * 
     * @type {number}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    weightOz?: number | null;
    /**
     * 
     * @type {CreateMagicScanSynthesizeResponsePayloadDimensions}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    dimensions?: CreateMagicScanSynthesizeResponsePayloadDimensions | null;
    /**
     * Echoed user-selected price (cents). Set on every synthesize call
     * so the master form's defaultPrice input gets populated regardless
     * of which path (AI or heuristic) generated the rest.
     * @type {number}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    priceCents?: number | null;
    /**
     * The seller's own words about this item — the pre-scan hint, or the
     * post-scan note that supersedes it (see synthesize/seller-note.ts).
     * Carried on the payload so the TAXONOMY resolvers see it too: category
     * and facet picking happen in their own LLM calls, which never saw the
     * note even after the main synthesis prompt did. A seller correcting the
     * variant was still getting the category of the wrong one.
     * @type {string}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    sellerNote?: string | null;
    /**
     * Comp price band (cents), outlier-trimmed — low/median/high across the
     * matched comps. The UI surfaces the range so the seller prices
     * strategically instead of trusting one number; priceCents defaults to
     * the median when the seller hasn't picked.
     * @type {number}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    priceLowCents?: number | null;
    /**
     * 
     * @type {number}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    priceMedianCents?: number | null;
    /**
     * 
     * @type {number}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    priceHighCents?: number | null;
    /**
     * Echoed seller-supplied listable-unit count from Magic List. This is
     * the listing's ADVERTISED stock (how many of this listing to sell).
     * Hydrates the form's `quantity` field. Undefined = leave form default (1).
     * @type {number}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    quantity?: number | null;
    /**
     * Physical units the seller actually has in stock — becomes the auto-
     * created inventory item's quantity/quantityAvailable. Distinct from
     * `quantity` (advertised stock) and `unitsPerListing` (composition).
     * Undefined = fall back to `quantity`.
     * @type {number}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    inventoryQuantity?: number | null;
    /**
     * How many physical units are bundled inside ONE listing (composition;
     * e.g. a pack of 4). Becomes listing_inventory_items.quantity so the
     * multi-channel fulfillable = floor(inventoryQuantity / unitsPerListing).
     * Undefined = 1.
     * @type {number}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    unitsPerListing?: number | null;
    /**
     * What the seller paid, per physical unit, in cents — the auto-created
     * inventory item's `costBasisCents`. Nothing wrote that column from any
     * create path, so every Magic List item had a NULL cost basis and every
     * sale of one reported its full sale price as profit. Undefined = unknown,
     * which stays NULL (0 would be a claim that the item was free).
     * @type {number}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    costBasisCents?: number | null;
    /**
     * 
     * @type {CreateMagicScanSynthesizeResponsePayloadChosenDimensions}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    chosenDimensions?: CreateMagicScanSynthesizeResponsePayloadChosenDimensions | null;
    /**
     * Every photo the seller uploaded for the source scan, primary
     * first. The NewListingPage hydrator drops these straight into the
     * form's images state so the seller doesn't have to re-upload.
     * @type {Array<string>}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    imageUrls?: Array<string> | null;
    /**
     * 
     * @type {CreateMagicScanSynthesizeResponsePayloadCategory}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    category?: CreateMagicScanSynthesizeResponsePayloadCategory | null;
    /**
     * eBay item-specifics: Department/Gender/Style/Pattern/Type. These
     * drive the eBay-required aspects on the form's Item Details
     * section. Mostly only eBay matches have them.
     * @type {string}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    department?: string | null;
    /**
     * 
     * @type {string}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    gender?: string | null;
    /**
     * 
     * @type {string}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    style?: string | null;
    /**
     * 
     * @type {string}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    pattern?: string | null;
    /**
     * 
     * @type {string}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    itemType?: string | null;
    /**
     * Universal product code, when eBay's enriched detail surfaced it.
     * Hydrates the master form's UPC/GTIN field.
     * @type {string}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    upc?: string | null;
    /**
     * 
     * @type {string}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    handlingTimeDays?: string | null;
    /**
     * 
     * @type {string}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    returnWindowDays?: string | null;
    /**
     * 
     * @type {Array<string>}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    tags?: Array<string> | null;
    /**
     * Master-level item specifics (JSON Record<string,string[]>). Item
     * specifics are now master-owned + fanned to every aspect platform, so the
     * form's shared Item-specifics editor prefills from this. Mirrored from the
     * richest resolved per-platform specifics blob (eBay after full-aspect
     * enrichment). Undefined = no specifics.
     * @type {string}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    itemSpecifics?: string | null;
    /**
     * Per-platform required-fields map — slotted directly into
     * platformOverrides when the form hydrates.
     * @type {object}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    perPlatformOverrides: object;
    /**
     * AI's confidence in the synthesis (0-1). Surfaced to the seller
     * so they can decide whether to skim or trust + click.
     * @type {number}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    confidence: number;
    /**
     * AI's free-text rationale for the merge — what it pulled from
     * where. Helps the seller spot a bad merge before listing.
     * @type {string}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    notes?: string | null;
    /**
     * 
     * @type {CreateMagicScanSynthesizeResponsePayloadSectionApplicability}
     * @memberof CreateMagicScanSynthesizeResponsePayload
     */
    sectionApplicability?: CreateMagicScanSynthesizeResponsePayloadSectionApplicability | null;
}

export function CreateMagicScanSynthesizeResponsePayloadFromJSON(json: any): CreateMagicScanSynthesizeResponsePayload {
    return CreateMagicScanSynthesizeResponsePayloadFromJSONTyped(json, false);
}

export function CreateMagicScanSynthesizeResponsePayloadFromJSONTyped(json: any, ignoreDiscriminator: boolean): CreateMagicScanSynthesizeResponsePayload {
    if ((json === undefined) || (json === null)) {
        return json;
    }
    return {
        
        'title': json['title'],
        'description': json['description'],
        'brand': !exists(json, 'brand') ? undefined : json['brand'],
        'grading': !exists(json, 'grading') ? undefined : CreateMagicScanSynthesizeResponsePayloadGradingFromJSON(json['grading']),
        'color': !exists(json, 'color') ? undefined : json['color'],
        'material': !exists(json, 'material') ? undefined : json['material'],
        'size': !exists(json, 'size') ? undefined : json['size'],
        'sizeSystem': !exists(json, 'sizeSystem') ? undefined : json['sizeSystem'],
        'weightOz': !exists(json, 'weightOz') ? undefined : json['weightOz'],
        'dimensions': !exists(json, 'dimensions') ? undefined : CreateMagicScanSynthesizeResponsePayloadDimensionsFromJSON(json['dimensions']),
        'priceCents': !exists(json, 'priceCents') ? undefined : json['priceCents'],
        'sellerNote': !exists(json, 'sellerNote') ? undefined : json['sellerNote'],
        'priceLowCents': !exists(json, 'priceLowCents') ? undefined : json['priceLowCents'],
        'priceMedianCents': !exists(json, 'priceMedianCents') ? undefined : json['priceMedianCents'],
        'priceHighCents': !exists(json, 'priceHighCents') ? undefined : json['priceHighCents'],
        'quantity': !exists(json, 'quantity') ? undefined : json['quantity'],
        'inventoryQuantity': !exists(json, 'inventoryQuantity') ? undefined : json['inventoryQuantity'],
        'unitsPerListing': !exists(json, 'unitsPerListing') ? undefined : json['unitsPerListing'],
        'costBasisCents': !exists(json, 'costBasisCents') ? undefined : json['costBasisCents'],
        'chosenDimensions': !exists(json, 'chosenDimensions') ? undefined : CreateMagicScanSynthesizeResponsePayloadChosenDimensionsFromJSON(json['chosenDimensions']),
        'imageUrls': !exists(json, 'imageUrls') ? undefined : json['imageUrls'],
        'category': !exists(json, 'category') ? undefined : CreateMagicScanSynthesizeResponsePayloadCategoryFromJSON(json['category']),
        'department': !exists(json, 'department') ? undefined : json['department'],
        'gender': !exists(json, 'gender') ? undefined : json['gender'],
        'style': !exists(json, 'style') ? undefined : json['style'],
        'pattern': !exists(json, 'pattern') ? undefined : json['pattern'],
        'itemType': !exists(json, 'itemType') ? undefined : json['itemType'],
        'upc': !exists(json, 'upc') ? undefined : json['upc'],
        'handlingTimeDays': !exists(json, 'handlingTimeDays') ? undefined : json['handlingTimeDays'],
        'returnWindowDays': !exists(json, 'returnWindowDays') ? undefined : json['returnWindowDays'],
        'tags': !exists(json, 'tags') ? undefined : json['tags'],
        'itemSpecifics': !exists(json, 'itemSpecifics') ? undefined : json['itemSpecifics'],
        'perPlatformOverrides': json['perPlatformOverrides'],
        'confidence': json['confidence'],
        'notes': !exists(json, 'notes') ? undefined : json['notes'],
        'sectionApplicability': !exists(json, 'sectionApplicability') ? undefined : CreateMagicScanSynthesizeResponsePayloadSectionApplicabilityFromJSON(json['sectionApplicability']),
    };
}

export function CreateMagicScanSynthesizeResponsePayloadToJSON(value?: CreateMagicScanSynthesizeResponsePayload | null): any {
    if (value === undefined) {
        return undefined;
    }
    if (value === null) {
        return null;
    }
    return {
        
        'title': value.title,
        'description': value.description,
        'brand': value.brand,
        'grading': CreateMagicScanSynthesizeResponsePayloadGradingToJSON(value.grading),
        'color': value.color,
        'material': value.material,
        'size': value.size,
        'sizeSystem': value.sizeSystem,
        'weightOz': value.weightOz,
        'dimensions': CreateMagicScanSynthesizeResponsePayloadDimensionsToJSON(value.dimensions),
        'priceCents': value.priceCents,
        'sellerNote': value.sellerNote,
        'priceLowCents': value.priceLowCents,
        'priceMedianCents': value.priceMedianCents,
        'priceHighCents': value.priceHighCents,
        'quantity': value.quantity,
        'inventoryQuantity': value.inventoryQuantity,
        'unitsPerListing': value.unitsPerListing,
        'costBasisCents': value.costBasisCents,
        'chosenDimensions': CreateMagicScanSynthesizeResponsePayloadChosenDimensionsToJSON(value.chosenDimensions),
        'imageUrls': value.imageUrls,
        'category': CreateMagicScanSynthesizeResponsePayloadCategoryToJSON(value.category),
        'department': value.department,
        'gender': value.gender,
        'style': value.style,
        'pattern': value.pattern,
        'itemType': value.itemType,
        'upc': value.upc,
        'handlingTimeDays': value.handlingTimeDays,
        'returnWindowDays': value.returnWindowDays,
        'tags': value.tags,
        'itemSpecifics': value.itemSpecifics,
        'perPlatformOverrides': value.perPlatformOverrides,
        'confidence': value.confidence,
        'notes': value.notes,
        'sectionApplicability': CreateMagicScanSynthesizeResponsePayloadSectionApplicabilityToJSON(value.sectionApplicability),
    };
}

