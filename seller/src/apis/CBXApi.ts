/* tslint:disable */
/* eslint-disable */
/**
 * Crossly Seller API
 * The seller-facing Crossly API — listings, crossposting, orders, analytics. Authenticate with a Personal Access Token (`Authorization: Bearer crossly_pat_…`) or a seller OAuth token (`crossly_oat_…`), scoped `<domain>:<read|write>`. Buyer endpoints are a SEPARATE API with a separate principal — see the Crossly Buyer API.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import {
    CreateCbxAccrualPurchaseResponse,
    CreateCbxAccrualPurchaseResponseFromJSON,
    CreateCbxAccrualPurchaseResponseToJSON,
    CreateCbxAccrualResponse,
    CreateCbxAccrualResponseFromJSON,
    CreateCbxAccrualResponseToJSON,
    CreateCbxAccrualReverseResponse,
    CreateCbxAccrualReverseResponseFromJSON,
    CreateCbxAccrualReverseResponseToJSON,
    CreateCbxAdCreditPurchaseResponse,
    CreateCbxAdCreditPurchaseResponseFromJSON,
    CreateCbxAdCreditPurchaseResponseToJSON,
    CreateCbxAdCreditQuoteResponse,
    CreateCbxAdCreditQuoteResponseFromJSON,
    CreateCbxAdCreditQuoteResponseToJSON,
    CreateCbxAdCreditSpendResponse,
    CreateCbxAdCreditSpendResponseFromJSON,
    CreateCbxAdCreditSpendResponseToJSON,
    CreateCbxBoostPauseResponse,
    CreateCbxBoostPauseResponseFromJSON,
    CreateCbxBoostPauseResponseToJSON,
    CreateCbxBoostResponse,
    CreateCbxBoostResponseFromJSON,
    CreateCbxBoostResponseToJSON,
    CreateCbxCampaignApproveResponse,
    CreateCbxCampaignApproveResponseFromJSON,
    CreateCbxCampaignApproveResponseToJSON,
    CreateCbxCampaignExecuteResponse,
    CreateCbxCampaignExecuteResponseFromJSON,
    CreateCbxCampaignExecuteResponseToJSON,
    CreateCbxCampaignPreviewResponse,
    CreateCbxCampaignPreviewResponseFromJSON,
    CreateCbxCampaignPreviewResponseToJSON,
    CreateCbxCampaignResponse,
    CreateCbxCampaignResponseFromJSON,
    CreateCbxCampaignResponseToJSON,
    CreateCbxClaimQuoteResponse,
    CreateCbxClaimQuoteResponseFromJSON,
    CreateCbxClaimQuoteResponseToJSON,
    CreateCbxClaimResponse,
    CreateCbxClaimResponseFromJSON,
    CreateCbxClaimResponseToJSON,
    CreateCbxClaimSendResponse,
    CreateCbxClaimSendResponseFromJSON,
    CreateCbxClaimSendResponseToJSON,
    CreateCbxCreditDrawResponse,
    CreateCbxCreditDrawResponseFromJSON,
    CreateCbxCreditDrawResponseToJSON,
    CreateCbxCreditFreezeResponse,
    CreateCbxCreditFreezeResponseFromJSON,
    CreateCbxCreditFreezeResponseToJSON,
    CreateCbxCreditRefreshResponse,
    CreateCbxCreditRefreshResponseFromJSON,
    CreateCbxCreditRefreshResponseToJSON,
    CreateCbxCreditRepayResponse,
    CreateCbxCreditRepayResponseFromJSON,
    CreateCbxCreditRepayResponseToJSON,
    CreateCbxDisbursementRuleActiveResponse,
    CreateCbxDisbursementRuleActiveResponseFromJSON,
    CreateCbxDisbursementRuleActiveResponseToJSON,
    CreateCbxDisbursementRuleCheckResponse,
    CreateCbxDisbursementRuleCheckResponseFromJSON,
    CreateCbxDisbursementRuleCheckResponseToJSON,
    CreateCbxDisbursementRuleResponse,
    CreateCbxDisbursementRuleResponseFromJSON,
    CreateCbxDisbursementRuleResponseToJSON,
    CreateCbxEarnTierResponse,
    CreateCbxEarnTierResponseFromJSON,
    CreateCbxEarnTierResponseToJSON,
    CreateCbxRateQuoteResponse,
    CreateCbxRateQuoteResponseFromJSON,
    CreateCbxRateQuoteResponseToJSON,
    CreateCbxRedemptionQuoteResponse,
    CreateCbxRedemptionQuoteResponseFromJSON,
    CreateCbxRedemptionQuoteResponseToJSON,
    CreateCbxRedemptionResponse,
    CreateCbxRedemptionResponseFromJSON,
    CreateCbxRedemptionResponseToJSON,
    CreateCbxRevenueSweepResponse,
    CreateCbxRevenueSweepResponseFromJSON,
    CreateCbxRevenueSweepResponseToJSON,
    CreateCbxSpendResponse,
    CreateCbxSpendResponseFromJSON,
    CreateCbxSpendResponseToJSON,
    CreateCbxSpendReverseResponse,
    CreateCbxSpendReverseResponseFromJSON,
    CreateCbxSpendReverseResponseToJSON,
    CreateCbxStakeTierResponse,
    CreateCbxStakeTierResponseFromJSON,
    CreateCbxStakeTierResponseToJSON,
    CreateCbxSubjectGrantResponse,
    CreateCbxSubjectGrantResponseFromJSON,
    CreateCbxSubjectGrantResponseToJSON,
    CreateCbxSubjectResponse,
    CreateCbxSubjectResponseFromJSON,
    CreateCbxSubjectResponseToJSON,
    CreateCbxSubjectSpendPlanResponse,
    CreateCbxSubjectSpendPlanResponseFromJSON,
    CreateCbxSubjectSpendPlanResponseToJSON,
    CreateCbxSubjectStakeResponse,
    CreateCbxSubjectStakeResponseFromJSON,
    CreateCbxSubjectStakeResponseToJSON,
    CreateCbxSubjectStakeUnstakeResponse,
    CreateCbxSubjectStakeUnstakeResponseFromJSON,
    CreateCbxSubjectStakeUnstakeResponseToJSON,
    CreateCbxWalletChallengeResponse,
    CreateCbxWalletChallengeResponseFromJSON,
    CreateCbxWalletChallengeResponseToJSON,
    CreateCbxWalletPaymentConfirmResponse,
    CreateCbxWalletPaymentConfirmResponseFromJSON,
    CreateCbxWalletPaymentConfirmResponseToJSON,
    CreateCbxWalletPaymentQuoteResponse,
    CreateCbxWalletPaymentQuoteResponseFromJSON,
    CreateCbxWalletPaymentQuoteResponseToJSON,
    CreateCbxWalletPaymentResolveResponse,
    CreateCbxWalletPaymentResolveResponseFromJSON,
    CreateCbxWalletPaymentResolveResponseToJSON,
    CreateCbxWalletVerifyResponse,
    CreateCbxWalletVerifyResponseFromJSON,
    CreateCbxWalletVerifyResponseToJSON,
    GetCbxAdCreditResponse,
    GetCbxAdCreditResponseFromJSON,
    GetCbxAdCreditResponseToJSON,
    GetCbxClaimResponse,
    GetCbxClaimResponseFromJSON,
    GetCbxClaimResponseToJSON,
    GetCbxCreditResponse,
    GetCbxCreditResponseFromJSON,
    GetCbxCreditResponseToJSON,
    GetCbxMeResponse,
    GetCbxMeResponseFromJSON,
    GetCbxMeResponseToJSON,
    GetCbxPoolResponse,
    GetCbxPoolResponseFromJSON,
    GetCbxPoolResponseToJSON,
    GetCbxRevenueResponse,
    GetCbxRevenueResponseFromJSON,
    GetCbxRevenueResponseToJSON,
    GetCbxSubjectBalanceBySubjectIdResponse,
    GetCbxSubjectBalanceBySubjectIdResponseFromJSON,
    GetCbxSubjectBalanceBySubjectIdResponseToJSON,
    GetCbxSubjectBalanceResponse,
    GetCbxSubjectBalanceResponseFromJSON,
    GetCbxSubjectBalanceResponseToJSON,
    GetCbxSubjectSpentResponse,
    GetCbxSubjectSpentResponseFromJSON,
    GetCbxSubjectSpentResponseToJSON,
    GetCbxSubjectStakeResponse,
    GetCbxSubjectStakeResponseFromJSON,
    GetCbxSubjectStakeResponseToJSON,
    GetCbxSubjectWalletResponse,
    GetCbxSubjectWalletResponseFromJSON,
    GetCbxSubjectWalletResponseToJSON,
    GetCbxTreasuryResponse,
    GetCbxTreasuryResponseFromJSON,
    GetCbxTreasuryResponseToJSON,
    V1List,
    V1ListFromJSON,
    V1ListToJSON,
} from '../models';

export interface CreateCbxAccrualReverseRequest {
    accrualId: string;
}

export interface CreateCbxBoostPauseRequest {
    boostId: string;
}

export interface CreateCbxCampaignApproveRequest {
    campaignId: string;
}

export interface CreateCbxCampaignExecuteRequest {
    campaignId: string;
}

export interface CreateCbxCampaignPreviewRequest {
    campaignId: string;
}

export interface CreateCbxClaimSendRequest {
    claimId: string;
}

export interface CreateCbxDisbursementRuleActiveRequest {
    ruleId: string;
}

export interface CreateCbxDisbursementRuleCheckRequest {
    ruleId: string;
}

export interface CreateCbxSpendReverseRequest {
    externalId: string;
}

export interface CreateCbxSubjectGrantRequest {
    subjectId: string;
}

export interface CreateCbxSubjectSpendPlanRequest {
    subjectId: string;
}

export interface CreateCbxSubjectStakeRequest {
    subjectId: string;
}

export interface CreateCbxSubjectStakeUnstakeRequest {
    subjectId: string;
}

export interface CreateCbxWalletPaymentResolveRequest {
    paymentId: string;
}

export interface GetCbxClaimRequest {
    claimId: string;
}

export interface GetCbxSubjectBalanceRequest {
    subjectId: string;
}

export interface GetCbxSubjectBalanceBySubjectIdRequest {
    subjectId: string;
}

export interface GetCbxSubjectSpentRequest {
    subjectId: string;
}

export interface GetCbxSubjectStakeRequest {
    subjectId: string;
}

export interface GetCbxSubjectWalletRequest {
    subjectId: string;
}

export interface ListCbxCampaignPayoutsRequest {
    campaignId: string;
}

export interface ListCbxSubjectGrantsRequest {
    subjectId: string;
}

export interface ListCbxSubjectLedgerRequest {
    subjectId: string;
    limit?: number;
}

/**
 * 
 */
export class CBXApi extends runtime.BaseAPI {

    /**
     * The accrual is PENDING until its maturation window closes, then converts to CBX at that moment\'s market price. It is denominated in cents the whole time it is pending, deliberately: quoting a token quantity up front and buying later would leave the reserve short for the length of the window. Send sourceExternalId and a retry is a no-op rather than a double credit.
     * Record cashback a user earned, in cents.
     */
    async createCbxAccrualRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxAccrualResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/accruals`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxAccrualResponseFromJSON(jsonValue));
    }

    /**
     * The accrual is PENDING until its maturation window closes, then converts to CBX at that moment\'s market price. It is denominated in cents the whole time it is pending, deliberately: quoting a token quantity up front and buying later would leave the reserve short for the length of the window. Send sourceExternalId and a retry is a no-op rather than a double credit.
     * Record cashback a user earned, in cents.
     */
    async createCbxAccrual(initOverrides?: RequestInit): Promise<CreateCbxAccrualResponse> {
        const response = await this.createCbxAccrualRaw(initOverrides);
        return await response.value();
    }

    /**
     * Prefer this over POST /accruals when you want us to do the rate maths — it resolves the tier, the boost and the stake boost, charges your boost budget in the same transaction, and records what rate was actually granted. If a matching boost cannot fund the order, the accrual falls back to your un-boosted rate and `boostBudgetExhausted` is true. Idempotent on externalId: a retried webhook neither accrues twice nor charges your budget twice.
     * Accrue cashback for an order at the resolved rate.
     */
    async createCbxAccrualPurchaseRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxAccrualPurchaseResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/accruals/purchase`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxAccrualPurchaseResponseFromJSON(jsonValue));
    }

    /**
     * Prefer this over POST /accruals when you want us to do the rate maths — it resolves the tier, the boost and the stake boost, charges your boost budget in the same transaction, and records what rate was actually granted. If a matching boost cannot fund the order, the accrual falls back to your un-boosted rate and `boostBudgetExhausted` is true. Idempotent on externalId: a retried webhook neither accrues twice nor charges your budget twice.
     * Accrue cashback for an order at the resolved rate.
     */
    async createCbxAccrualPurchase(initOverrides?: RequestInit): Promise<CreateCbxAccrualPurchaseResponse> {
        const response = await this.createCbxAccrualPurchaseRaw(initOverrides);
        return await response.value();
    }

    /**
     * Only works while the accrual is still pending. Once it has converted, the value is tokens in somebody\'s balance and this is the wrong operation: reversing then is a debit against that balance, which is a different act with different consequences and is not something an integration key can do. This is why the maturation window has to be at least as long as your refund window.
     * Claw back a pending accrual — a refund, a cancellation, fraud.
     */
    async createCbxAccrualReverseRaw(requestParameters: CreateCbxAccrualReverseRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxAccrualReverseResponse>> {
        if (requestParameters.accrualId === null || requestParameters.accrualId === undefined) {
            throw new runtime.RequiredError('accrualId','Required parameter requestParameters.accrualId was null or undefined when calling createCbxAccrualReverse.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/accruals/{accrualId}/reverse`.replace(`{${"accrualId"}}`, encodeURIComponent(String(requestParameters.accrualId))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxAccrualReverseResponseFromJSON(jsonValue));
    }

    /**
     * Only works while the accrual is still pending. Once it has converted, the value is tokens in somebody\'s balance and this is the wrong operation: reversing then is a debit against that balance, which is a different act with different consequences and is not something an integration key can do. This is why the maturation window has to be at least as long as your refund window.
     * Claw back a pending accrual — a refund, a cancellation, fraud.
     */
    async createCbxAccrualReverse(requestParameters: CreateCbxAccrualReverseRequest, initOverrides?: RequestInit): Promise<CreateCbxAccrualReverseResponse> {
        const response = await this.createCbxAccrualReverseRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Send CBX to our revenue wallet yourself, then present the signature. We read the actual balance delta at FINALIZED commitment — a confirmed transaction can still be dropped by a fork, and this grants real credit. One signature can be claimed exactly once. Priced at the spot when the claim is processed, not when you signed: pricing at send time would let somebody hold signed transfers and claim only the ones that moved in their favour. A 409 means the transaction has not finalized yet and you should retry; a 400 means it will never be claimable (failed, wrong mint, wrong destination).
     * Claim ad credit against a CBX transfer you sent.
     */
    async createCbxAdCreditPurchaseRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxAdCreditPurchaseResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/ad-credit/purchase`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxAdCreditPurchaseResponseFromJSON(jsonValue));
    }

    /**
     * Send CBX to our revenue wallet yourself, then present the signature. We read the actual balance delta at FINALIZED commitment — a confirmed transaction can still be dropped by a fork, and this grants real credit. One signature can be claimed exactly once. Priced at the spot when the claim is processed, not when you signed: pricing at send time would let somebody hold signed transfers and claim only the ones that moved in their favour. A 409 means the transaction has not finalized yet and you should retry; a 400 means it will never be claimable (failed, wrong mint, wrong destination).
     * Claim ad credit against a CBX transfer you sent.
     */
    async createCbxAdCreditPurchase(initOverrides?: RequestInit): Promise<CreateCbxAdCreditPurchaseResponse> {
        const response = await this.createCbxAdCreditPurchaseRaw(initOverrides);
        return await response.value();
    }

    /**
     * Credit is 1:1 with the market value of the tokens at confirmation. Refuses with 409 when there is no fresh price — pricing an entire prepaid budget off a guess is not something to do quietly.
     * What a given number of tokens buys in ad credit.
     */
    async createCbxAdCreditQuoteRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxAdCreditQuoteResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/ad-credit/quote`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxAdCreditQuoteResponseFromJSON(jsonValue));
    }

    /**
     * Credit is 1:1 with the market value of the tokens at confirmation. Refuses with 409 when there is no fresh price — pricing an entire prepaid budget off a guess is not something to do quietly.
     * What a given number of tokens buys in ad credit.
     */
    async createCbxAdCreditQuote(initOverrides?: RequestInit): Promise<CreateCbxAdCreditQuoteResponse> {
        const response = await this.createCbxAdCreditQuoteRaw(initOverrides);
        return await response.value();
    }

    /**
     * Spends what the balance covers and reports the rest as `shortfallCents` — the campaign should stop there rather than running on credit that does not exist. 20% of what is spent moves to the community events pool and 80% is operator revenue; the split happens on SPEND rather than at purchase, because the pool\'s share is earned when the advertising is actually delivered. Idempotent on externalId: pass your billing-period id so a retried run does not consume the same credit twice.
     * Consume credit for a billing period.
     */
    async createCbxAdCreditSpendRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxAdCreditSpendResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/ad-credit/spend`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxAdCreditSpendResponseFromJSON(jsonValue));
    }

    /**
     * Spends what the balance covers and reports the rest as `shortfallCents` — the campaign should stop there rather than running on credit that does not exist. 20% of what is spent moves to the community events pool and 80% is operator revenue; the split happens on SPEND rather than at purchase, because the pool\'s share is earned when the advertising is actually delivered. Idempotent on externalId: pass your billing-period id so a retried run does not consume the same credit twice.
     * Consume credit for a billing period.
     */
    async createCbxAdCreditSpend(initOverrides?: RequestInit): Promise<CreateCbxAdCreditSpendResponse> {
        const response = await this.createCbxAdCreditSpendRaw(initOverrides);
        return await response.value();
    }

    /**
     * Performance marketing priced in CBX: the spend lands as a durable balance the buyer returns to use, rather than a one-time price cut they pocket. A boost REPLACES the base or tier rate rather than adding to it — you are stating the total you will pay, and it is priced against your margin. Targeting is matched by equality on one facet; most specific wins (sku > collection > category > all). Kinds: all, category, sku, collection.
     * Fund elevated cashback on matching items.
     */
    async createCbxBoostRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxBoostResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/boosts`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxBoostResponseFromJSON(jsonValue));
    }

    /**
     * Performance marketing priced in CBX: the spend lands as a durable balance the buyer returns to use, rather than a one-time price cut they pocket. A boost REPLACES the base or tier rate rather than adding to it — you are stating the total you will pay, and it is priced against your margin. Targeting is matched by equality on one facet; most specific wins (sku > collection > category > all). Kinds: all, category, sku, collection.
     * Fund elevated cashback on matching items.
     */
    async createCbxBoost(initOverrides?: RequestInit): Promise<CreateCbxBoostResponse> {
        const response = await this.createCbxBoostRaw(initOverrides);
        return await response.value();
    }

    /**
     * Pausing does not refund anything — already-accrued cashback is a promise already made to a buyer, and unwinding it would take back cashback somebody was shown at checkout. The remaining budget simply stops being spendable.
     * Stop a boost from matching further orders.
     */
    async createCbxBoostPauseRaw(requestParameters: CreateCbxBoostPauseRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxBoostPauseResponse>> {
        if (requestParameters.boostId === null || requestParameters.boostId === undefined) {
            throw new runtime.RequiredError('boostId','Required parameter requestParameters.boostId was null or undefined when calling createCbxBoostPause.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/boosts/{boostId}/pause`.replace(`{${"boostId"}}`, encodeURIComponent(String(requestParameters.boostId))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxBoostPauseResponseFromJSON(jsonValue));
    }

    /**
     * Pausing does not refund anything — already-accrued cashback is a promise already made to a buyer, and unwinding it would take back cashback somebody was shown at checkout. The remaining budget simply stops being spendable.
     * Stop a boost from matching further orders.
     */
    async createCbxBoostPause(requestParameters: CreateCbxBoostPauseRequest, initOverrides?: RequestInit): Promise<CreateCbxBoostPauseResponse> {
        const response = await this.createCbxBoostPauseRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Nothing is paid until it is previewed, approved and executed. Available metrics: accruals_count, accrued_cents, spend_count, spend_base_units — all of them measure activity inside the window. `capPerSubject` is worth setting on a proportional campaign: without it one large participant can take almost the whole pool, which makes the event pointless for everybody else.
     * Create a campaign in draft.
     */
    async createCbxCampaignRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxCampaignResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/campaigns`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxCampaignResponseFromJSON(jsonValue));
    }

    /**
     * Nothing is paid until it is previewed, approved and executed. Available metrics: accruals_count, accrued_cents, spend_count, spend_base_units — all of them measure activity inside the window. `capPerSubject` is worth setting on a proportional campaign: without it one large participant can take almost the whole pool, which makes the event pointless for everybody else.
     * Create a campaign in draft.
     */
    async createCbxCampaign(initOverrides?: RequestInit): Promise<CreateCbxCampaignResponse> {
        const response = await this.createCbxCampaignRaw(initOverrides);
        return await response.value();
    }

    /**
     * Approve the previewed recipient list.
     */
    async createCbxCampaignApproveRaw(requestParameters: CreateCbxCampaignApproveRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxCampaignApproveResponse>> {
        if (requestParameters.campaignId === null || requestParameters.campaignId === undefined) {
            throw new runtime.RequiredError('campaignId','Required parameter requestParameters.campaignId was null or undefined when calling createCbxCampaignApprove.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/campaigns/{campaignId}/approve`.replace(`{${"campaignId"}}`, encodeURIComponent(String(requestParameters.campaignId))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxCampaignApproveResponseFromJSON(jsonValue));
    }

    /**
     * Approve the previewed recipient list.
     */
    async createCbxCampaignApprove(requestParameters: CreateCbxCampaignApproveRequest, initOverrides?: RequestInit): Promise<CreateCbxCampaignApproveResponse> {
        const response = await this.createCbxCampaignApproveRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Recomputes the list and refuses if the hash no longer matches the approved one. Payouts credit balances directly rather than transferring on chain, so a distribution to ten thousand recipients costs one internal move and is reversible if it was computed wrong.
     * Pay an approved campaign.
     */
    async createCbxCampaignExecuteRaw(requestParameters: CreateCbxCampaignExecuteRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxCampaignExecuteResponse>> {
        if (requestParameters.campaignId === null || requestParameters.campaignId === undefined) {
            throw new runtime.RequiredError('campaignId','Required parameter requestParameters.campaignId was null or undefined when calling createCbxCampaignExecute.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/campaigns/{campaignId}/execute`.replace(`{${"campaignId"}}`, encodeURIComponent(String(requestParameters.campaignId))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxCampaignExecuteResponseFromJSON(jsonValue));
    }

    /**
     * Recomputes the list and refuses if the hash no longer matches the approved one. Payouts credit balances directly rather than transferring on chain, so a distribution to ten thousand recipients costs one internal move and is reversible if it was computed wrong.
     * Pay an approved campaign.
     */
    async createCbxCampaignExecute(requestParameters: CreateCbxCampaignExecuteRequest, initOverrides?: RequestInit): Promise<CreateCbxCampaignExecuteResponse> {
        const response = await this.createCbxCampaignExecuteRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every recipient and amount, plus a hash of the list. Re-previewing invalidates any prior approval by design — the approver signed off on a specific list, and if it has changed they have not approved what would now happen.
     * Compute the recipient list without paying it.
     */
    async createCbxCampaignPreviewRaw(requestParameters: CreateCbxCampaignPreviewRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxCampaignPreviewResponse>> {
        if (requestParameters.campaignId === null || requestParameters.campaignId === undefined) {
            throw new runtime.RequiredError('campaignId','Required parameter requestParameters.campaignId was null or undefined when calling createCbxCampaignPreview.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/campaigns/{campaignId}/preview`.replace(`{${"campaignId"}}`, encodeURIComponent(String(requestParameters.campaignId))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxCampaignPreviewResponseFromJSON(jsonValue));
    }

    /**
     * Returns every recipient and amount, plus a hash of the list. Re-previewing invalidates any prior approval by design — the approver signed off on a specific list, and if it has changed they have not approved what would now happen.
     * Compute the recipient list without paying it.
     */
    async createCbxCampaignPreview(requestParameters: CreateCbxCampaignPreviewRequest, initOverrides?: RequestInit): Promise<CreateCbxCampaignPreviewResponse> {
        const response = await this.createCbxCampaignPreviewRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * The balance is debited here, before anything is sent, which is the only safe order: sending first and debiting after means a crash between the two pays somebody and never charges them, and that is unrecoverable. A crash after this leaves a reserved balance and a pending claim, which is recoverable by looking at the chain. Send the same idempotencyKey to retry safely.
     * Reserve a claim. Debits the balance and queues the transfer.
     */
    async createCbxClaimRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxClaimResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/claims`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxClaimResponseFromJSON(jsonValue));
    }

    /**
     * The balance is debited here, before anything is sent, which is the only safe order: sending first and debiting after means a crash between the two pays somebody and never charges them, and that is unrecoverable. A crash after this leaves a reserved balance and a pending claim, which is recoverable by looking at the chain. Send the same idempotencyKey to retry safely.
     * Reserve a claim. Debits the balance and queues the transfer.
     */
    async createCbxClaim(initOverrides?: RequestInit): Promise<CreateCbxClaimResponse> {
        const response = await this.createCbxClaimRaw(initOverrides);
        return await response.value();
    }

    /**
     * Every fee at cost, so a confirm screen can show the breakdown before the user agrees. The network fee is passed through at actual cost and includes the one-time account rent when the recipient has no token account yet — that rent is a recoverable deposit on an account the USER owns, not a fee we keep.
     * What a claim would cost, without committing to it.
     */
    async createCbxClaimQuoteRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxClaimQuoteResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/claims/quote`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxClaimQuoteResponseFromJSON(jsonValue));
    }

    /**
     * Every fee at cost, so a confirm screen can show the breakdown before the user agrees. The network fee is passed through at actual cost and includes the one-time account rent when the recipient has no token account yet — that rent is a recoverable deposit on an account the USER owns, not a fee we keep.
     * What a claim would cost, without committing to it.
     */
    async createCbxClaimQuote(initOverrides?: RequestInit): Promise<CreateCbxClaimQuoteResponse> {
        const response = await this.createCbxClaimQuoteRaw(initOverrides);
        return await response.value();
    }

    /**
     * Re-checks every precondition rather than trusting the reservation: the delegation may have been revoked, the fee payer may have run dry, the address may have been flagged since. A response of `unconfirmed` means the transfer may have landed but confirmation was not observed — do NOT retry it, it needs reconciliation against the chain first.
     * Send a reserved claim on chain.
     */
    async createCbxClaimSendRaw(requestParameters: CreateCbxClaimSendRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxClaimSendResponse>> {
        if (requestParameters.claimId === null || requestParameters.claimId === undefined) {
            throw new runtime.RequiredError('claimId','Required parameter requestParameters.claimId was null or undefined when calling createCbxClaimSend.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/claims/{claimId}/send`.replace(`{${"claimId"}}`, encodeURIComponent(String(requestParameters.claimId))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxClaimSendResponseFromJSON(jsonValue));
    }

    /**
     * Re-checks every precondition rather than trusting the reservation: the delegation may have been revoked, the fee payer may have run dry, the address may have been flagged since. A response of `unconfirmed` means the transfer may have landed but confirmation was not observed — do NOT retry it, it needs reconciliation against the chain first.
     * Send a reserved claim on chain.
     */
    async createCbxClaimSend(requestParameters: CreateCbxClaimSendRequest, initOverrides?: RequestInit): Promise<CreateCbxClaimSendResponse> {
        const response = await this.createCbxClaimSendRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * The advance lands as GRANT balance: in-platform only, so it cannot be withdrawn, cannot be turned into cash and absconded with, and adds nothing to the float that could hit an order book. Restricted to wholesale channels — the limit was sized on the theory that the advance buys goods that get sold and generate the payout stream repaying it, and credit spent on a subscription does not create that stream. Refuses if the reserve has no unallocated tokens: credit is real value and cannot be advanced against tokens that do not exist. Idempotent on externalId.
     * Draw against a line, receiving grant credit.
     */
    async createCbxCreditDrawRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxCreditDrawResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/credit/draw`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxCreditDrawResponseFromJSON(jsonValue));
    }

    /**
     * The advance lands as GRANT balance: in-platform only, so it cannot be withdrawn, cannot be turned into cash and absconded with, and adds nothing to the float that could hit an order book. Restricted to wholesale channels — the limit was sized on the theory that the advance buys goods that get sold and generate the payout stream repaying it, and credit spent on a subscription does not create that stream. Refuses if the reserve has no unallocated tokens: credit is real value and cannot be advanced against tokens that do not exist. Idempotent on externalId.
     * Draw against a line, receiving grant credit.
     */
    async createCbxCreditDraw(initOverrides?: RequestInit): Promise<CreateCbxCreditDrawResponse> {
        const response = await this.createCbxCreditDrawRaw(initOverrides);
        return await response.value();
    }

    /**
     * The only lever over a line, and deliberately the only one. A seller who took inventory on Tuesday keeps Tuesday\'s terms whatever the token does on Wednesday — the only way a credit product sits next to a volatile asset without transmitting its volatility.
     * Stop new draws. Leaves the drawn balance on its terms.
     */
    async createCbxCreditFreezeRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxCreditFreezeResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/credit/freeze`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxCreditFreezeResponseFromJSON(jsonValue));
    }

    /**
     * The only lever over a line, and deliberately the only one. A seller who took inventory on Tuesday keeps Tuesday\'s terms whatever the token does on Wednesday — the only way a credit product sits next to a volatile asset without transmitting its volatility.
     * Stop new draws. Leaves the drawn balance on its terms.
     */
    async createCbxCreditFreeze(initOverrides?: RequestInit): Promise<CreateCbxCreditFreezeResponse> {
        const response = await this.createCbxCreditFreezeRaw(initOverrides);
        return await response.value();
    }

    /**
     * The earned limit is a share of trailing SETTLED payout volume — money that actually reached the seller, not listed inventory or projected sales. The stake bonus is capped at a share of that, so a seller with no history gets nothing however much they stake. A frozen line stays frozen: freezing is a credit decision somebody made, and a recompute must not quietly undo it.
     * Recompute a limit from trading history and stake.
     */
    async createCbxCreditRefreshRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxCreditRefreshResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/credit/refresh`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxCreditRefreshResponseFromJSON(jsonValue));
    }

    /**
     * The earned limit is a share of trailing SETTLED payout volume — money that actually reached the seller, not listed inventory or projected sales. The stake bonus is capped at a share of that, so a seller with no history gets nothing however much they stake. A frozen line stays frozen: freezing is a credit decision somebody made, and a recompute must not quietly undo it.
     * Recompute a limit from trading history and stake.
     */
    async createCbxCreditRefresh(initOverrides?: RequestInit): Promise<CreateCbxCreditRefreshResponse> {
        const response = await this.createCbxCreditRefreshRaw(initOverrides);
        return await response.value();
    }

    /**
     * `treasury` scope, which looks backwards next to a draw needing only `spend` and is deliberate: a forged repayment writes off real money owed to us, while a forged draw hands out credit spendable only in our own marketplace. The scope follows the loss. Clamped to what is outstanding — a payout larger than the debt would otherwise push the balance negative and read as credit nobody underwrote.
     * Apply a repayment to a line.
     */
    async createCbxCreditRepayRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxCreditRepayResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/credit/repay`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxCreditRepayResponseFromJSON(jsonValue));
    }

    /**
     * `treasury` scope, which looks backwards next to a draw needing only `spend` and is deliberate: a forged repayment writes off real money owed to us, while a forged draw hands out credit spendable only in our own marketplace. The scope follows the loss. Clamped to what is outstanding — a payout larger than the debt would otherwise push the balance negative and read as credit nobody underwrote.
     * Apply a repayment to a line.
     */
    async createCbxCreditRepay(initOverrides?: RequestInit): Promise<CreateCbxCreditRepayResponse> {
        const response = await this.createCbxCreditRepayRaw(initOverrides);
        return await response.value();
    }

    /**
     * Applies to the EVENTS POOL only — never free reserve surplus. Reserve surplus is the recirculation buffer that funds the next accrual without touching the market, so distributing it would force us to buy the same tokens back at spread plus MEV. `checkCadenceHours` bounds how often the rule may fire even when the pool is over the line: a pure threshold fires at an unpredictable moment, and the pool jumps most after a lapse sweep — precisely when engagement was worst. `distributeBps` is capped under 100% because draining the pool removes the standing balance that makes the next event credible. The rule decides WHEN only: firing opens a campaign that still needs preview, approval and execution. Metrics: accruals_count, accrued_cents, spend_count, spend_base_units.
     * Fire a distribution when the events pool crosses a threshold.
     */
    async createCbxDisbursementRuleRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxDisbursementRuleResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/disbursement-rules`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxDisbursementRuleResponseFromJSON(jsonValue));
    }

    /**
     * Applies to the EVENTS POOL only — never free reserve surplus. Reserve surplus is the recirculation buffer that funds the next accrual without touching the market, so distributing it would force us to buy the same tokens back at spread plus MEV. `checkCadenceHours` bounds how often the rule may fire even when the pool is over the line: a pure threshold fires at an unpredictable moment, and the pool jumps most after a lapse sweep — precisely when engagement was worst. `distributeBps` is capped under 100% because draining the pool removes the standing balance that makes the next event credible. The rule decides WHEN only: firing opens a campaign that still needs preview, approval and execution. Metrics: accruals_count, accrued_cents, spend_count, spend_base_units.
     * Fire a distribution when the events pool crosses a threshold.
     */
    async createCbxDisbursementRule(initOverrides?: RequestInit): Promise<CreateCbxDisbursementRuleResponse> {
        const response = await this.createCbxDisbursementRuleRaw(initOverrides);
        return await response.value();
    }

    /**
     * Enable or disable a rule.
     */
    async createCbxDisbursementRuleActiveRaw(requestParameters: CreateCbxDisbursementRuleActiveRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxDisbursementRuleActiveResponse>> {
        if (requestParameters.ruleId === null || requestParameters.ruleId === undefined) {
            throw new runtime.RequiredError('ruleId','Required parameter requestParameters.ruleId was null or undefined when calling createCbxDisbursementRuleActive.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/disbursement-rules/{ruleId}/active`.replace(`{${"ruleId"}}`, encodeURIComponent(String(requestParameters.ruleId))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxDisbursementRuleActiveResponseFromJSON(jsonValue));
    }

    /**
     * Enable or disable a rule.
     */
    async createCbxDisbursementRuleActive(requestParameters: CreateCbxDisbursementRuleActiveRequest, initOverrides?: RequestInit): Promise<CreateCbxDisbursementRuleActiveResponse> {
        const response = await this.createCbxDisbursementRuleActiveRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Gates in order: cadence, threshold, coverage. `outcome` names the one that stopped it — every evaluation is recorded including the declines, because \"why didn\'t the event happen\" is the question people ask and the answer is always a check that ran and said no. A missing or stale treasury snapshot declines on `coverage`: unknown coverage is not healthy coverage. Send dryRun to evaluate without opening a campaign or writing anything.
     * Evaluate a rule now. Fires it if every gate passes.
     */
    async createCbxDisbursementRuleCheckRaw(requestParameters: CreateCbxDisbursementRuleCheckRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxDisbursementRuleCheckResponse>> {
        if (requestParameters.ruleId === null || requestParameters.ruleId === undefined) {
            throw new runtime.RequiredError('ruleId','Required parameter requestParameters.ruleId was null or undefined when calling createCbxDisbursementRuleCheck.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/disbursement-rules/{ruleId}/check`.replace(`{${"ruleId"}}`, encodeURIComponent(String(requestParameters.ruleId))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxDisbursementRuleCheckResponseFromJSON(jsonValue));
    }

    /**
     * Gates in order: cadence, threshold, coverage. `outcome` names the one that stopped it — every evaluation is recorded including the declines, because \"why didn\'t the event happen\" is the question people ask and the answer is always a check that ran and said no. A missing or stale treasury snapshot declines on `coverage`: unknown coverage is not healthy coverage. Send dryRun to evaluate without opening a campaign or writing anything.
     * Evaluate a rule now. Fires it if every gate passes.
     */
    async createCbxDisbursementRuleCheck(requestParameters: CreateCbxDisbursementRuleCheckRequest, initOverrides?: RequestInit): Promise<CreateCbxDisbursementRuleCheckResponse> {
        const response = await this.createCbxDisbursementRuleCheckRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Setting `isDefault` moves the default off whatever held it — exactly one active default per merchant is enforced by a unique index, because two would make \"what rate did this user get\" depend on row order.
     * Define an earn term.
     */
    async createCbxEarnTierRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxEarnTierResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/earn-tiers`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxEarnTierResponseFromJSON(jsonValue));
    }

    /**
     * Setting `isDefault` moves the default off whatever held it — exactly one active default per merchant is enforced by a unique index, because two would make \"what rate did this user get\" depend on row order.
     * Define an earn term.
     */
    async createCbxEarnTier(initOverrides?: RequestInit): Promise<CreateCbxEarnTierResponse> {
        const response = await this.createCbxEarnTierRaw(initOverrides);
        return await response.value();
    }

    /**
     * Resolve the rate without accruing anything, so a checkout can show the real number and its reason. `source` tells you whether it came from your base rate, a tier, or a boost; `stakeBoostBps` is the part the user\'s own stake contributed. Quoting does NOT reserve boost budget — a quote and the subsequent accrual can differ if the budget runs out in between, which is why the accrual response repeats the rate it actually granted.
     * What would this order earn, and why.
     */
    async createCbxRateQuoteRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxRateQuoteResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/rates/quote`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxRateQuoteResponseFromJSON(jsonValue));
    }

    /**
     * Resolve the rate without accruing anything, so a checkout can show the real number and its reason. `source` tells you whether it came from your base rate, a tier, or a boost; `stakeBoostBps` is the part the user\'s own stake contributed. Quoting does NOT reserve boost budget — a quote and the subsequent accrual can differ if the budget runs out in between, which is why the accrual response repeats the rate it actually granted.
     * What would this order earn, and why.
     */
    async createCbxRateQuote(initOverrides?: RequestInit): Promise<CreateCbxRateQuoteResponse> {
        const response = await this.createCbxRateQuoteRaw(initOverrides);
        return await response.value();
    }

    /**
     * Idempotent on (serviceKind, externalId) rather than externalId alone: a grading submission and a listing boost can legitimately share an id because they refer to the same item, and without the service in the key, boosting a listing you had already graded would return the grading receipt and never charge for the boost. Staked tokens cannot pay — the debit checks spendable balance, not total.
     * Pay for a service in CBX.
     */
    async createCbxRedemptionRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxRedemptionResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/redemptions`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxRedemptionResponseFromJSON(jsonValue));
    }

    /**
     * Idempotent on (serviceKind, externalId) rather than externalId alone: a grading submission and a listing boost can legitimately share an id because they refer to the same item, and without the service in the key, boosting a listing you had already graded would return the grading receipt and never charge for the boost. Staked tokens cannot pay — the debit checks spendable balance, not total.
     * Pay for a service in CBX.
     */
    async createCbxRedemption(initOverrides?: RequestInit): Promise<CreateCbxRedemptionResponse> {
        const response = await this.createCbxRedemptionRaw(initOverrides);
        return await response.value();
    }

    /**
     * Refuses with 409 when there is no fresh price. A dollar-priced service has no honest token quantity without a spot, and there is no safe direction to guess in — a guess either overcharges the user or undercharges us.
     * What a service costs in tokens right now.
     */
    async createCbxRedemptionQuoteRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxRedemptionQuoteResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/redemptions/quote`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxRedemptionQuoteResponseFromJSON(jsonValue));
    }

    /**
     * Refuses with 409 when there is no fresh price. A dollar-priced service has no honest token quantity without a spot, and there is no safe direction to guess in — a guess either overcharges the user or undercharges us.
     * What a service costs in tokens right now.
     */
    async createCbxRedemptionQuote(initOverrides?: RequestInit): Promise<CreateCbxRedemptionQuoteResponse> {
        const response = await this.createCbxRedemptionQuoteRaw(initOverrides);
        return await response.value();
    }

    /**
     * Computes what is genuinely free — reserve minus outstanding balances, minus claims in flight, minus the pool, minus unswept revenue — and moves at most that. If the reserve is short it moves NOTHING, whatever the ledger says: an under-covered reserve is not a reason to stop paying users, it is a reason to stop paying yourself. Send dryRun to see the arithmetic without moving tokens.
     * Move accrued revenue from the reserve to your revenue wallet.
     */
    async createCbxRevenueSweepRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxRevenueSweepResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/revenue/sweep`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxRevenueSweepResponseFromJSON(jsonValue));
    }

    /**
     * Computes what is genuinely free — reserve minus outstanding balances, minus claims in flight, minus the pool, minus unswept revenue — and moves at most that. If the reserve is short it moves NOTHING, whatever the ledger says: an under-covered reserve is not a reason to stop paying users, it is a reason to stop paying yourself. Send dryRun to see the arithmetic without moving tokens.
     * Move accrued revenue from the reserve to your revenue wallet.
     */
    async createCbxRevenueSweep(initOverrides?: RequestInit): Promise<CreateCbxRevenueSweepResponse> {
        const response = await this.createCbxRevenueSweepRaw(initOverrides);
        return await response.value();
    }

    /**
     * Debits the user exactly what they spent — the skim is never added on top, because making CBX worth less when used than when sold would invert the whole reason to spend rather than liquidate. The skim comes out of YOUR fee on the order and is capped against it, so an order paid entirely in saved-up CBX does not have its whole margin eaten. Idempotent on externalId: a retried checkout returns the original spend rather than debiting twice. Spending is always free and has no minimum — that asymmetry against the withdrawal fee is what steers toward spending without ever telling anyone they cannot have their money.
     * Redeem a user\'s CBX against an order.
     */
    async createCbxSpendRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxSpendResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/spends`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxSpendResponseFromJSON(jsonValue));
    }

    /**
     * Debits the user exactly what they spent — the skim is never added on top, because making CBX worth less when used than when sold would invert the whole reason to spend rather than liquidate. The skim comes out of YOUR fee on the order and is capped against it, so an order paid entirely in saved-up CBX does not have its whole margin eaten. Idempotent on externalId: a retried checkout returns the original spend rather than debiting twice. Spending is always free and has no minimum — that asymmetry against the withdrawal fee is what steers toward spending without ever telling anyone they cannot have their money.
     * Redeem a user\'s CBX against an order.
     */
    async createCbxSpend(initOverrides?: RequestInit): Promise<CreateCbxSpendResponse> {
        const response = await this.createCbxSpendRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the user\'s tokens AND reverses the skim out of both the community pool and operator revenue. All three move together: returning the tokens while the pool and operator kept their shares would count the same tokens twice against one reserve. The spend also stops counting as activity for campaign weighting, so buy-then-refund cannot farm distributions.
     * Refund a spend — give the tokens back and claw the skim back.
     */
    async createCbxSpendReverseRaw(requestParameters: CreateCbxSpendReverseRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxSpendReverseResponse>> {
        if (requestParameters.externalId === null || requestParameters.externalId === undefined) {
            throw new runtime.RequiredError('externalId','Required parameter requestParameters.externalId was null or undefined when calling createCbxSpendReverse.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/spends/{externalId}/reverse`.replace(`{${"externalId"}}`, encodeURIComponent(String(requestParameters.externalId))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxSpendReverseResponseFromJSON(jsonValue));
    }

    /**
     * Returns the user\'s tokens AND reverses the skim out of both the community pool and operator revenue. All three move together: returning the tokens while the pool and operator kept their shares would count the same tokens twice against one reserve. The spend also stops counting as activity for campaign weighting, so buy-then-refund cannot farm distributions.
     * Refund a spend — give the tokens back and claw the skim back.
     */
    async createCbxSpendReverse(requestParameters: CreateCbxSpendReverseRequest, initOverrides?: RequestInit): Promise<CreateCbxSpendReverseResponse> {
        const response = await this.createCbxSpendReverseRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * A user gets the highest tier their amount clears. `cooldownDays` is what makes the discount real: without a wait, a user stakes for the discount and unstakes the moment they want to withdraw, so the commitment it was priced against never existed.
     * Define a staking tier.
     */
    async createCbxStakeTierRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxStakeTierResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/stake-tiers`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxStakeTierResponseFromJSON(jsonValue));
    }

    /**
     * A user gets the highest tier their amount clears. `cooldownDays` is what makes the discount real: without a wait, a user stakes for the discount and unstakes the moment they want to withdraw, so the commitment it was priced against never existed.
     * Define a staking tier.
     */
    async createCbxStakeTier(initOverrides?: RequestInit): Promise<CreateCbxStakeTierResponse> {
        const response = await this.createCbxStakeTierRaw(initOverrides);
        return await response.value();
    }

    /**
     * Idempotent. Call it whenever you need a subject id; repeated calls with the same externalUserId return the same subject. Your user ids are opaque to us and unique only within your merchant, so two marketplaces can both have a user \"1\" without collision.
     * Map one of your user ids to a CBX subject.
     */
    async createCbxSubjectRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxSubjectResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/subjects`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxSubjectResponseFromJSON(jsonValue));
    }

    /**
     * Idempotent. Call it whenever you need a subject id; repeated calls with the same externalUserId return the same subject. Your user ids are opaque to us and unique only within your merchant, so two marketplaces can both have a user \"1\" without collision.
     * Map one of your user ids to a CBX subject.
     */
    async createCbxSubject(initOverrides?: RequestInit): Promise<CreateCbxSubjectResponse> {
        const response = await this.createCbxSubjectRaw(initOverrides);
        return await response.value();
    }

    /**
     * Requires a funding `batchId` for every kind except `grant_makegood`. Grant credit is spendable at merchants who receive real value, so the tokens have to exist — the same rule earned balance obeys. A makegood is exempt because compensating somebody for our failure must not be blocked on treasury state. There is no path that converts a grant to earned balance or pays it to an address, and the database enforces that rather than a comment.
     * Issue grant credit — in-platform, non-withdrawable.
     */
    async createCbxSubjectGrantRaw(requestParameters: CreateCbxSubjectGrantRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxSubjectGrantResponse>> {
        if (requestParameters.subjectId === null || requestParameters.subjectId === undefined) {
            throw new runtime.RequiredError('subjectId','Required parameter requestParameters.subjectId was null or undefined when calling createCbxSubjectGrant.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/subjects/{subjectId}/grants`.replace(`{${"subjectId"}}`, encodeURIComponent(String(requestParameters.subjectId))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxSubjectGrantResponseFromJSON(jsonValue));
    }

    /**
     * Requires a funding `batchId` for every kind except `grant_makegood`. Grant credit is spendable at merchants who receive real value, so the tokens have to exist — the same rule earned balance obeys. A makegood is exempt because compensating somebody for our failure must not be blocked on treasury state. There is no path that converts a grant to earned balance or pays it to an address, and the database enforces that rather than a comment.
     * Issue grant credit — in-platform, non-withdrawable.
     */
    async createCbxSubjectGrant(requestParameters: CreateCbxSubjectGrantRequest, initOverrides?: RequestInit): Promise<CreateCbxSubjectGrantResponse> {
        const response = await this.createCbxSubjectGrantRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * The order is granted → earned → connected and you do not get to choose it. Granted first is a security property, not a preference: if earned spent first, a subject holding both would spend their withdrawable balance down while their non-withdrawable grant sat untouched — converting a grant into a withdrawable balance one purchase at a time. Returns a `shortfallBaseUnits` rather than failing, so a checkout can charge the remainder to a card. Send excludeConnected on a flow that cannot wait for an on-chain transfer.
     * Which balances would pay for a spend, and in what order.
     */
    async createCbxSubjectSpendPlanRaw(requestParameters: CreateCbxSubjectSpendPlanRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxSubjectSpendPlanResponse>> {
        if (requestParameters.subjectId === null || requestParameters.subjectId === undefined) {
            throw new runtime.RequiredError('subjectId','Required parameter requestParameters.subjectId was null or undefined when calling createCbxSubjectSpendPlan.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/subjects/{subjectId}/spend-plan`.replace(`{${"subjectId"}}`, encodeURIComponent(String(requestParameters.subjectId))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxSubjectSpendPlanResponseFromJSON(jsonValue));
    }

    /**
     * The order is granted → earned → connected and you do not get to choose it. Granted first is a security property, not a preference: if earned spent first, a subject holding both would spend their withdrawable balance down while their non-withdrawable grant sat untouched — converting a grant into a withdrawable balance one purchase at a time. Returns a `shortfallBaseUnits` rather than failing, so a checkout can charge the remainder to a card. Send excludeConnected on a flow that cannot wait for an on-chain transfer.
     * Which balances would pay for a spend, and in what order.
     */
    async createCbxSubjectSpendPlan(requestParameters: CreateCbxSubjectSpendPlanRequest, initOverrides?: RequestInit): Promise<CreateCbxSubjectSpendPlanResponse> {
        const response = await this.createCbxSubjectSpendPlanRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Refuses an amount that clears no tier — locking tokens for no benefit is never what somebody meant to do. One stake per subject: to change the amount, unstake and wait out the cooldown first.
     * Lock a subject\'s tokens for a tier.
     */
    async createCbxSubjectStakeRaw(requestParameters: CreateCbxSubjectStakeRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxSubjectStakeResponse>> {
        if (requestParameters.subjectId === null || requestParameters.subjectId === undefined) {
            throw new runtime.RequiredError('subjectId','Required parameter requestParameters.subjectId was null or undefined when calling createCbxSubjectStake.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/subjects/{subjectId}/stake`.replace(`{${"subjectId"}}`, encodeURIComponent(String(requestParameters.subjectId))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxSubjectStakeResponseFromJSON(jsonValue));
    }

    /**
     * Refuses an amount that clears no tier — locking tokens for no benefit is never what somebody meant to do. One stake per subject: to change the amount, unstake and wait out the cooldown first.
     * Lock a subject\'s tokens for a tier.
     */
    async createCbxSubjectStake(requestParameters: CreateCbxSubjectStakeRequest, initOverrides?: RequestInit): Promise<CreateCbxSubjectStakeResponse> {
        const response = await this.createCbxSubjectStakeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * The earn boost stops immediately; the tokens stay locked until `unlocksAt`. The cooldown is read from the tier as configured NOW, which is the one place a later config change is allowed to matter — holding somebody to a longer wait the merchant has since abandoned would be the worse behaviour.
     * Start the cooldown. Tokens unlock when it elapses.
     */
    async createCbxSubjectStakeUnstakeRaw(requestParameters: CreateCbxSubjectStakeUnstakeRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxSubjectStakeUnstakeResponse>> {
        if (requestParameters.subjectId === null || requestParameters.subjectId === undefined) {
            throw new runtime.RequiredError('subjectId','Required parameter requestParameters.subjectId was null or undefined when calling createCbxSubjectStakeUnstake.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/subjects/{subjectId}/stake/unstake`.replace(`{${"subjectId"}}`, encodeURIComponent(String(requestParameters.subjectId))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxSubjectStakeUnstakeResponseFromJSON(jsonValue));
    }

    /**
     * The earn boost stops immediately; the tokens stay locked until `unlocksAt`. The cooldown is read from the tier as configured NOW, which is the one place a later config change is allowed to matter — holding somebody to a longer wait the merchant has since abandoned would be the worse behaviour.
     * Start the cooldown. Tokens unlock when it elapses.
     */
    async createCbxSubjectStakeUnstake(requestParameters: CreateCbxSubjectStakeUnstakeRequest, initOverrides?: RequestInit): Promise<CreateCbxSubjectStakeUnstakeResponse> {
        const response = await this.createCbxSubjectStakeUnstakeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Present the returned `message` verbatim to the user\'s wallet for signing. It binds your merchant, their subject, the address and a single-use nonce, so the resulting signature is not transferable to another address or account. A signature is required because there is no custody here: a send cannot be undone, so a typo or a swapped address is permanent.
     * Start wallet verification. Returns a message for the user to sign.
     */
    async createCbxWalletChallengeRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxWalletChallengeResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/wallets/challenge`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxWalletChallengeResponseFromJSON(jsonValue));
    }

    /**
     * Present the returned `message` verbatim to the user\'s wallet for signing. It binds your merchant, their subject, the address and a single-use nonce, so the resulting signature is not transferable to another address or account. A signature is required because there is no custody here: a send cannot be undone, so a typo or a swapped address is permanent.
     * Start wallet verification. Returns a message for the user to sign.
     */
    async createCbxWalletChallenge(initOverrides?: RequestInit): Promise<CreateCbxWalletChallengeResponse> {
        const response = await this.createCbxWalletChallengeRaw(initOverrides);
        return await response.value();
    }

    /**
     * Everything is read from the CHAIN at finalized commitment — the amount, the payer, the destination. Nothing you assert about the payment is trusted, because a client that can state its own payment amount can state a larger one. The response `releaseDecision` is about the ORDER, not the payment: by the time we see a signature the tokens have moved and cannot be un-moved, so the only decision left is whether to hand over goods. `release` means ship. `review` means hold — a person needs to look, and that includes the case where no screening provider is configured. `refuse` means do not ship; the payment is still recorded, because we received the tokens and that fact does not go away. Idempotent twice over: on txSig globally, so one payment cannot pay two orders, and on (merchant, externalId), so one order is not paid twice. A 409 means the transaction has not finalized yet and you should retry; a 400 means it never will be claimable.
     * Present the signature. Returns a ship / do-not-ship decision.
     */
    async createCbxWalletPaymentConfirmRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxWalletPaymentConfirmResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/wallet-payments/confirm`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxWalletPaymentConfirmResponseFromJSON(jsonValue));
    }

    /**
     * Everything is read from the CHAIN at finalized commitment — the amount, the payer, the destination. Nothing you assert about the payment is trusted, because a client that can state its own payment amount can state a larger one. The response `releaseDecision` is about the ORDER, not the payment: by the time we see a signature the tokens have moved and cannot be un-moved, so the only decision left is whether to hand over goods. `release` means ship. `review` means hold — a person needs to look, and that includes the case where no screening provider is configured. `refuse` means do not ship; the payment is still recorded, because we received the tokens and that fact does not go away. Idempotent twice over: on txSig globally, so one payment cannot pay two orders, and on (merchant, externalId), so one order is not paid twice. A 409 means the transaction has not finalized yet and you should retry; a 400 means it never will be claimable.
     * Present the signature. Returns a ship / do-not-ship decision.
     */
    async createCbxWalletPaymentConfirm(initOverrides?: RequestInit): Promise<CreateCbxWalletPaymentConfirmResponse> {
        const response = await this.createCbxWalletPaymentConfirmRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns an unsigned, base64 transaction. The buyer\'s wallet signs and submits it; we never hold a key or a delegation and never submit anything, so the platform has no authority over their tokens at any point. RESERVES NOTHING — no row, no hold, no balance change. The buyer may never sign it. The SIGNATURE is the event, so treat this as a convenience and not a commitment. `lastValidBlockHeight` is when it expires: a wallet prompt left open for a couple of minutes produces a transaction the chain will reject, and you should re-quote rather than retry. `payerCanCover` is a courtesy read of their balance so you can warn before a prompt rather than after a failure; null means we could not read it, which is not the same as \"no\". The buyer needs no prior wallet registration: a payment proves control of the tokens, which is what a connect-and-verify step would have been proving.
     * Build a transfer for the buyer to sign themselves.
     */
    async createCbxWalletPaymentQuoteRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxWalletPaymentQuoteResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/wallet-payments/quote`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxWalletPaymentQuoteResponseFromJSON(jsonValue));
    }

    /**
     * Returns an unsigned, base64 transaction. The buyer\'s wallet signs and submits it; we never hold a key or a delegation and never submit anything, so the platform has no authority over their tokens at any point. RESERVES NOTHING — no row, no hold, no balance change. The buyer may never sign it. The SIGNATURE is the event, so treat this as a convenience and not a commitment. `lastValidBlockHeight` is when it expires: a wallet prompt left open for a couple of minutes produces a transaction the chain will reject, and you should re-quote rather than retry. `payerCanCover` is a courtesy read of their balance so you can warn before a prompt rather than after a failure; null means we could not read it, which is not the same as \"no\". The buyer needs no prior wallet registration: a payment proves control of the tokens, which is what a connect-and-verify step would have been proving.
     * Build a transfer for the buyer to sign themselves.
     */
    async createCbxWalletPaymentQuote(initOverrides?: RequestInit): Promise<CreateCbxWalletPaymentQuoteResponse> {
        const response = await this.createCbxWalletPaymentQuoteRaw(initOverrides);
        return await response.value();
    }

    /**
     * Only moves a payment OUT of `review`, never between the other two. A refusal that could later be flipped to a release is an approval control with no teeth, and a release re-decided as a refusal after the goods shipped is a record that no longer describes what happened. The reviewer and their note are stored on the row, because this is the decision somebody will be asked to justify.
     * A human decides on a held payment.
     */
    async createCbxWalletPaymentResolveRaw(requestParameters: CreateCbxWalletPaymentResolveRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxWalletPaymentResolveResponse>> {
        if (requestParameters.paymentId === null || requestParameters.paymentId === undefined) {
            throw new runtime.RequiredError('paymentId','Required parameter requestParameters.paymentId was null or undefined when calling createCbxWalletPaymentResolve.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/wallet-payments/{paymentId}/resolve`.replace(`{${"paymentId"}}`, encodeURIComponent(String(requestParameters.paymentId))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxWalletPaymentResolveResponseFromJSON(jsonValue));
    }

    /**
     * Only moves a payment OUT of `review`, never between the other two. A refusal that could later be flipped to a release is an approval control with no teeth, and a release re-decided as a refusal after the goods shipped is a record that no longer describes what happened. The reviewer and their note are stored on the row, because this is the decision somebody will be asked to justify.
     * A human decides on a held payment.
     */
    async createCbxWalletPaymentResolve(requestParameters: CreateCbxWalletPaymentResolveRequest, initOverrides?: RequestInit): Promise<CreateCbxWalletPaymentResolveResponse> {
        const response = await this.createCbxWalletPaymentResolveRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * The signature is checked against the message WE issued and stored, never one supplied here. The nonce is single-use, so the same signature cannot re-verify an address later.
     * Complete wallet verification with the user\'s signature.
     */
    async createCbxWalletVerifyRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateCbxWalletVerifyResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/wallets/verify`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateCbxWalletVerifyResponseFromJSON(jsonValue));
    }

    /**
     * The signature is checked against the message WE issued and stored, never one supplied here. The nonce is single-use, so the same signature cannot re-verify an address later.
     * Complete wallet verification with the user\'s signature.
     */
    async createCbxWalletVerify(initOverrides?: RequestInit): Promise<CreateCbxWalletVerifyResponse> {
        const response = await this.createCbxWalletVerifyRaw(initOverrides);
        return await response.value();
    }

    /**
     * Advertising is payable in CBX and nothing else. Your budget is denominated in dollars, priced at the spot when your payment finalized — deliberately NOT held as a token quantity, since a price move would otherwise silently change the budget you prepaid, making you a market participant because you bought ads. Credit is 1:1 with market value: with CBX the only way to pay there is nothing to discount against. Spendable on advertising only, and not refundable.
     * Unspent advertising credit, in cents.
     */
    async getCbxAdCreditRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetCbxAdCreditResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/ad-credit`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetCbxAdCreditResponseFromJSON(jsonValue));
    }

    /**
     * Advertising is payable in CBX and nothing else. Your budget is denominated in dollars, priced at the spot when your payment finalized — deliberately NOT held as a token quantity, since a price move would otherwise silently change the budget you prepaid, making you a market participant because you bought ads. Credit is 1:1 with market value: with CBX the only way to pay there is nothing to discount against. Spendable on advertising only, and not refundable.
     * Unspent advertising credit, in cents.
     */
    async getCbxAdCredit(initOverrides?: RequestInit): Promise<GetCbxAdCreditResponse> {
        const response = await this.getCbxAdCreditRaw(initOverrides);
        return await response.value();
    }

    /**
     * A claim\'s current state.
     */
    async getCbxClaimRaw(requestParameters: GetCbxClaimRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetCbxClaimResponse>> {
        if (requestParameters.claimId === null || requestParameters.claimId === undefined) {
            throw new runtime.RequiredError('claimId','Required parameter requestParameters.claimId was null or undefined when calling getCbxClaim.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/claims/{claimId}`.replace(`{${"claimId"}}`, encodeURIComponent(String(requestParameters.claimId))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetCbxClaimResponseFromJSON(jsonValue));
    }

    /**
     * A claim\'s current state.
     */
    async getCbxClaim(requestParameters: GetCbxClaimRequest, initOverrides?: RequestInit): Promise<GetCbxClaimResponse> {
        const response = await this.getCbxClaimRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Trade credit, not token-collateralized lending. The line is secured by receivables we already hold — the payout stream sits under a hold with an exposure ceiling — and secondarily by goods bought from our own wholesale channel. CBX is the alignment mechanism, not the collateral: a stake raises the limit and lowers the rate, bounded to a share of the earned limit so a price collapse can never remove the majority of a facility. The limit may FALL. A drawn balance is never accelerated or margin-called — there is no liquidation engine, no keeper and no oracle trigger anywhere in it.
     * A seller\'s wholesale credit line.
     */
    async getCbxCreditRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetCbxCreditResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/credit`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetCbxCreditResponseFromJSON(jsonValue));
    }

    /**
     * Trade credit, not token-collateralized lending. The line is secured by receivables we already hold — the payout stream sits under a hold with an exposure ceiling — and secondarily by goods bought from our own wholesale channel. CBX is the alignment mechanism, not the collateral: a stake raises the limit and lowers the rate, bounded to a share of the earned limit so a price collapse can never remove the majority of a facility. The limit may FALL. A drawn balance is never accelerated or margin-called — there is no liquidation engine, no keeper and no oracle trigger anywhere in it.
     * A seller\'s wholesale credit line.
     */
    async getCbxCredit(initOverrides?: RequestInit): Promise<GetCbxCreditResponse> {
        const response = await this.getCbxCreditRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the economics your merchant is configured with, so an integration can display accurate terms rather than hard-coding ours. Note `claimsEnabled`: off means balances are store credit that cannot leave, which is the default and the smaller regulatory posture.
     * Identity check — which merchant this key belongs to, and its terms.
     */
    async getCbxMeRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetCbxMeResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/me`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetCbxMeResponseFromJSON(jsonValue));
    }

    /**
     * Returns the economics your merchant is configured with, so an integration can display accurate terms rather than hard-coding ours. Note `claimsEnabled`: off means balances are store credit that cannot leave, which is the default and the smaller regulatory posture.
     * Identity check — which merchant this key belongs to, and its terms.
     */
    async getCbxMe(initOverrides?: RequestInit): Promise<GetCbxMeResponse> {
        const response = await this.getCbxMeRaw(initOverrides);
        return await response.value();
    }

    /**
     * Funded by the skim on in-marketplace CBX spending, the community share of withdrawal fees, and lapsed balances. Sponsor budgets are tracked separately and are not included here.
     * Your events-pool balance.
     */
    async getCbxPoolRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetCbxPoolResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/pool`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetCbxPoolResponseFromJSON(jsonValue));
    }

    /**
     * Funded by the skim on in-marketplace CBX spending, the community share of withdrawal fees, and lapsed balances. Sponsor budgets are tracked separately and are not included here.
     * Your events-pool balance.
     */
    async getCbxPool(initOverrides?: RequestInit): Promise<GetCbxPoolResponse> {
        const response = await this.getCbxPoolRaw(initOverrides);
        return await response.value();
    }

    /**
     * Your share of the spend skim plus your half of withdrawal fees, denominated in CBX. It sits inside the reserve until swept, which is why it is tracked here rather than inferred: without a number saying how much of the reserve is yours, there is no safe amount to take out.
     * Operator revenue accrued and not yet withdrawn.
     */
    async getCbxRevenueRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetCbxRevenueResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/revenue`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetCbxRevenueResponseFromJSON(jsonValue));
    }

    /**
     * Your share of the spend skim plus your half of withdrawal fees, denominated in CBX. It sits inside the reserve until swept, which is why it is tracked here rather than inferred: without a number saying how much of the reserve is yours, there is no safe amount to take out.
     * Operator revenue accrued and not yet withdrawn.
     */
    async getCbxRevenue(initOverrides?: RequestInit): Promise<GetCbxRevenueResponse> {
        const response = await this.getCbxRevenueRaw(initOverrides);
        return await response.value();
    }

    /**
     * Two numbers because they are two different things. `pendingCents` is cashback earned but still inside its window — reversible, denominated in dollars, not yet tokens. `availableBaseUnits` is CBX they hold now. Amounts are strings because a token balance can exceed what a JSON number represents exactly.
     * What a subject holds: pending cents and available CBX.
     */
    async getCbxSubjectBalanceRaw(requestParameters: GetCbxSubjectBalanceRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetCbxSubjectBalanceResponse>> {
        if (requestParameters.subjectId === null || requestParameters.subjectId === undefined) {
            throw new runtime.RequiredError('subjectId','Required parameter requestParameters.subjectId was null or undefined when calling getCbxSubjectBalance.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/subjects/{subjectId}/balance`.replace(`{${"subjectId"}}`, encodeURIComponent(String(requestParameters.subjectId))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetCbxSubjectBalanceResponseFromJSON(jsonValue));
    }

    /**
     * Two numbers because they are two different things. `pendingCents` is cashback earned but still inside its window — reversible, denominated in dollars, not yet tokens. `availableBaseUnits` is CBX they hold now. Amounts are strings because a token balance can exceed what a JSON number represents exactly.
     * What a subject holds: pending cents and available CBX.
     */
    async getCbxSubjectBalance(requestParameters: GetCbxSubjectBalanceRequest, initOverrides?: RequestInit): Promise<GetCbxSubjectBalanceResponse> {
        const response = await this.getCbxSubjectBalanceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * EARNED is cashback and affiliate accruals — withdrawable once matured and above the claim floor. GRANTED is ad credit, wholesale draws and promos — spendable in your marketplace only, never withdrawable, so it creates no sell pressure. CONNECTED is the subject\'s own self-custodied CBX, reachable through a bounded delegation; it was never our liability and does not enter the reserve invariant. `connectedAvailableBaseUnits` is delegation HEADROOM, not a wallet balance — the subject may hold less than they approved, or have revoked on chain without telling us, so treat it as a ceiling and let the spend re-read the chain.
     * All three balances a subject holds.
     */
    async getCbxSubjectBalanceBySubjectIdRaw(requestParameters: GetCbxSubjectBalanceBySubjectIdRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetCbxSubjectBalanceBySubjectIdResponse>> {
        if (requestParameters.subjectId === null || requestParameters.subjectId === undefined) {
            throw new runtime.RequiredError('subjectId','Required parameter requestParameters.subjectId was null or undefined when calling getCbxSubjectBalanceBySubjectId.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/subjects/{subjectId}/balances`.replace(`{${"subjectId"}}`, encodeURIComponent(String(requestParameters.subjectId))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetCbxSubjectBalanceBySubjectIdResponseFromJSON(jsonValue));
    }

    /**
     * EARNED is cashback and affiliate accruals — withdrawable once matured and above the claim floor. GRANTED is ad credit, wholesale draws and promos — spendable in your marketplace only, never withdrawable, so it creates no sell pressure. CONNECTED is the subject\'s own self-custodied CBX, reachable through a bounded delegation; it was never our liability and does not enter the reserve invariant. `connectedAvailableBaseUnits` is delegation HEADROOM, not a wallet balance — the subject may hold less than they approved, or have revoked on chain without telling us, so treat it as a ceiling and let the spend re-read the chain.
     * All three balances a subject holds.
     */
    async getCbxSubjectBalanceBySubjectId(requestParameters: GetCbxSubjectBalanceBySubjectIdRequest, initOverrides?: RequestInit): Promise<GetCbxSubjectBalanceBySubjectIdResponse> {
        const response = await this.getCbxSubjectBalanceBySubjectIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Total CBX a subject has spent in your marketplace.
     */
    async getCbxSubjectSpentRaw(requestParameters: GetCbxSubjectSpentRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetCbxSubjectSpentResponse>> {
        if (requestParameters.subjectId === null || requestParameters.subjectId === undefined) {
            throw new runtime.RequiredError('subjectId','Required parameter requestParameters.subjectId was null or undefined when calling getCbxSubjectSpent.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/subjects/{subjectId}/spent`.replace(`{${"subjectId"}}`, encodeURIComponent(String(requestParameters.subjectId))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetCbxSubjectSpentResponseFromJSON(jsonValue));
    }

    /**
     * Total CBX a subject has spent in your marketplace.
     */
    async getCbxSubjectSpent(requestParameters: GetCbxSubjectSpentRequest, initOverrides?: RequestInit): Promise<GetCbxSubjectSpentResponse> {
        const response = await this.getCbxSubjectSpentRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * `availableBaseUnits` is the number a checkout must use — balance minus anything locked. `earnBoostBps` reads zero once an unstake has been requested, because the boost ends with the commitment; `feeDiscountBps` survives the cooldown, since withdrawing is exactly what somebody in cooldown is trying to do.
     * A subject\'s staking state and spendable balance.
     */
    async getCbxSubjectStakeRaw(requestParameters: GetCbxSubjectStakeRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetCbxSubjectStakeResponse>> {
        if (requestParameters.subjectId === null || requestParameters.subjectId === undefined) {
            throw new runtime.RequiredError('subjectId','Required parameter requestParameters.subjectId was null or undefined when calling getCbxSubjectStake.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/subjects/{subjectId}/stake`.replace(`{${"subjectId"}}`, encodeURIComponent(String(requestParameters.subjectId))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetCbxSubjectStakeResponseFromJSON(jsonValue));
    }

    /**
     * `availableBaseUnits` is the number a checkout must use — balance minus anything locked. `earnBoostBps` reads zero once an unstake has been requested, because the boost ends with the commitment; `feeDiscountBps` survives the cooldown, since withdrawing is exactly what somebody in cooldown is trying to do.
     * A subject\'s staking state and spendable balance.
     */
    async getCbxSubjectStake(requestParameters: GetCbxSubjectStakeRequest, initOverrides?: RequestInit): Promise<GetCbxSubjectStakeResponse> {
        const response = await this.getCbxSubjectStakeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * The verified payout address for a subject, if any.
     */
    async getCbxSubjectWalletRaw(requestParameters: GetCbxSubjectWalletRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetCbxSubjectWalletResponse>> {
        if (requestParameters.subjectId === null || requestParameters.subjectId === undefined) {
            throw new runtime.RequiredError('subjectId','Required parameter requestParameters.subjectId was null or undefined when calling getCbxSubjectWallet.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/subjects/{subjectId}/wallet`.replace(`{${"subjectId"}}`, encodeURIComponent(String(requestParameters.subjectId))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetCbxSubjectWalletResponseFromJSON(jsonValue));
    }

    /**
     * The verified payout address for a subject, if any.
     */
    async getCbxSubjectWallet(requestParameters: GetCbxSubjectWalletRequest, initOverrides?: RequestInit): Promise<GetCbxSubjectWalletResponse> {
        const response = await this.getCbxSubjectWalletRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Coverage is your reserve measured against what you owe your users, including claims already in flight. Below 100% your conversions stop — we will not credit balances that nothing backs. Claims and spends keep working, because those move value out and improve coverage.
     * Your most recent reserve reconciliation.
     */
    async getCbxTreasuryRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetCbxTreasuryResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/treasury`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetCbxTreasuryResponseFromJSON(jsonValue));
    }

    /**
     * Coverage is your reserve measured against what you owe your users, including claims already in flight. Below 100% your conversions stop — we will not credit balances that nothing backs. Claims and spends keep working, because those move value out and improve coverage.
     * Your most recent reserve reconciliation.
     */
    async getCbxTreasury(initOverrides?: RequestInit): Promise<GetCbxTreasuryResponse> {
        const response = await this.getCbxTreasuryRaw(initOverrides);
        return await response.value();
    }

    /**
     * Append-only. `purchase_cbx` and `grant` add; `ad_spend` and `expire` subtract. There is no refund kind and there will not be one: advertising credit exists to buy advertising, and any exit at face value turns it into a currency exchange.
     * Ad-credit movements, newest first.
     */
    async listCbxAdCreditLedgerRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/ad-credit/ledger`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * Append-only. `purchase_cbx` and `grant` add; `ad_spend` and `expire` subtract. There is no refund kind and there will not be one: advertising credit exists to buy advertising, and any exit at face value turns it into a currency exchange.
     * Ad-credit movements, newest first.
     */
    async listCbxAdCreditLedger(initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listCbxAdCreditLedgerRaw(initOverrides);
        return await response.value();
    }

    /**
     * `spentCents` against `budgetCents` is the live burn. The budget is a hard ceiling enforced inside the accrual transaction, so a boost cannot overspend — when it runs out, matching orders quietly fall back to your base rate rather than failing or accruing zero.
     * Your funded cashback boosts, newest first.
     */
    async listCbxBoostsRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/boosts`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * `spentCents` against `budgetCents` is the live burn. The budget is a hard ceiling enforced inside the accrual transaction, so a boost cannot overspend — when it runs out, matching orders quietly fall back to your base rate rather than failing or accruing zero.
     * Your funded cashback boosts, newest first.
     */
    async listCbxBoosts(initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listCbxBoostsRaw(initOverrides);
        return await response.value();
    }

    /**
     * The weight is kept so a payout can be explained to the person who received it. \"Why did I get this much\" should have an answer that is not \"the algorithm\".
     * What a campaign actually paid, with the weight behind each amount.
     */
    async listCbxCampaignPayoutsRaw(requestParameters: ListCbxCampaignPayoutsRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        if (requestParameters.campaignId === null || requestParameters.campaignId === undefined) {
            throw new runtime.RequiredError('campaignId','Required parameter requestParameters.campaignId was null or undefined when calling listCbxCampaignPayouts.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/campaigns/{campaignId}/payouts`.replace(`{${"campaignId"}}`, encodeURIComponent(String(requestParameters.campaignId))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * The weight is kept so a payout can be explained to the person who received it. \"Why did I get this much\" should have an answer that is not \"the algorithm\".
     * What a campaign actually paid, with the weight behind each amount.
     */
    async listCbxCampaignPayouts(requestParameters: ListCbxCampaignPayoutsRequest, initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listCbxCampaignPayoutsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Your campaigns, newest first.
     */
    async listCbxCampaignsRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/campaigns`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * Your campaigns, newest first.
     */
    async listCbxCampaigns(initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listCbxCampaignsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Safe to show users. A climbing counter toward a known number is the reason to prefer cadence-plus-threshold over a pure threshold: people can see the pool rising and know roughly when the next event is possible. A trigger nobody can anticipate generates suspicion rather than anticipation.
     * How close each rule is to firing — the public counter.
     */
    async listCbxDisbursementProgressRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/disbursement-progress`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * Safe to show users. A climbing counter toward a known number is the reason to prefer cadence-plus-threshold over a pure threshold: people can see the pool rising and know roughly when the next event is possible. A trigger nobody can anticipate generates suspicion rather than anticipation.
     * How close each rule is to firing — the public counter.
     */
    async listCbxDisbursementProgress(initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listCbxDisbursementProgressRaw(initOverrides);
        return await response.value();
    }

    /**
     * Threshold rules that fire community distributions.
     */
    async listCbxDisbursementRulesRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/disbursement-rules`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * Threshold rules that fire community distributions.
     */
    async listCbxDisbursementRules(initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listCbxDisbursementRulesRaw(initOverrides);
        return await response.value();
    }

    /**
     * A term structure on a rebate, not a yield: the user chooses WHEN to be paid for a purchase they already made, and a longer wait earns more. Nothing accrues to a balance for being held. Show these at checkout so the choice is the user\'s — the rate is snapshot onto the accrual, so a tier edited later never reprices a promise already made.
     * Earn terms on offer — longer maturation, higher rate.
     */
    async listCbxEarnTiersRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/earn-tiers`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * A term structure on a rebate, not a yield: the user chooses WHEN to be paid for a purchase they already made, and a longer wait earns more. Nothing accrues to a balance for being held. Show these at checkout so the choice is the user\'s — the rate is snapshot onto the accrual, so a tier edited later never reprices a promise already made.
     * Earn terms on offer — longer maturation, higher rate.
     */
    async listCbxEarnTiers(initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listCbxEarnTiersRaw(initOverrides);
        return await response.value();
    }

    /**
     * Paying in CBX costs less than paying in dollars, which is what makes anybody choose it. These are real services with real cost behind them rather than a sink invented to soak up supply — and sinks matter: supply is fixed, so tokens that only ever accumulate starve the market the protocol has to buy from.
     * Services payable in CBX, and the discount each carries.
     */
    async listCbxRedemptionServicesRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/redemptions/services`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * Paying in CBX costs less than paying in dollars, which is what makes anybody choose it. These are real services with real cost behind them rather than a sink invented to soak up supply — and sinks matter: supply is fixed, so tokens that only ever accumulate starve the market the protocol has to buy from.
     * Services payable in CBX, and the discount each carries.
     */
    async listCbxRedemptionServices(initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listCbxRedemptionServicesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Staking pays NOTHING. It confers a lower withdrawal fee and a higher earn rate on future purchases: a discount for commitment, not a return on a holding. Staked tokens never leave the user — they stay in their balance and stay backed by the reserve — they simply become unspendable until unstaked.
     * Staking tiers — what locking tokens buys.
     */
    async listCbxStakeTiersRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/stake-tiers`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * Staking pays NOTHING. It confers a lower withdrawal fee and a higher earn rate on future purchases: a discount for commitment, not a return on a holding. Staked tokens never leave the user — they stay in their balance and stay backed by the reserve — they simply become unspendable until unstaked.
     * Staking tiers — what locking tokens buys.
     */
    async listCbxStakeTiers(initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listCbxStakeTiersRaw(initOverrides);
        return await response.value();
    }

    /**
     * That ordering is the allocation order: a spend consumes the grant closest to lapsing, so value about to expire is used before value that will not. Grants with no expiry sort last.
     * Live grants, soonest-expiring first.
     */
    async listCbxSubjectGrantsRaw(requestParameters: ListCbxSubjectGrantsRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        if (requestParameters.subjectId === null || requestParameters.subjectId === undefined) {
            throw new runtime.RequiredError('subjectId','Required parameter requestParameters.subjectId was null or undefined when calling listCbxSubjectGrants.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/subjects/{subjectId}/grants`.replace(`{${"subjectId"}}`, encodeURIComponent(String(requestParameters.subjectId))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * That ordering is the allocation order: a spend consumes the grant closest to lapsing, so value about to expire is used before value that will not. Grants with no expiry sort last.
     * Live grants, soonest-expiring first.
     */
    async listCbxSubjectGrants(requestParameters: ListCbxSubjectGrantsRequest, initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listCbxSubjectGrantsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Append-only. The balance is the sum of these rows and there is no cached balance anywhere that could disagree with them.
     * A subject\'s CBX ledger, newest first.
     */
    async listCbxSubjectLedgerRaw(requestParameters: ListCbxSubjectLedgerRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        if (requestParameters.subjectId === null || requestParameters.subjectId === undefined) {
            throw new runtime.RequiredError('subjectId','Required parameter requestParameters.subjectId was null or undefined when calling listCbxSubjectLedger.');
        }

        const queryParameters: any = {};

        if (requestParameters.limit !== undefined) {
            queryParameters['limit'] = requestParameters.limit;
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/subjects/{subjectId}/ledger`.replace(`{${"subjectId"}}`, encodeURIComponent(String(requestParameters.subjectId))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * Append-only. The balance is the sum of these rows and there is no cached balance anywhere that could disagree with them.
     * A subject\'s CBX ledger, newest first.
     */
    async listCbxSubjectLedger(requestParameters: ListCbxSubjectLedgerRequest, initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listCbxSubjectLedgerRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Every row here is money taken and goods not shipped, which is not a state to leave a buyer in without it appearing on a list. `riskLevel` and `riskExposures` are the verdict as recorded at the time, not re-derived — asking a provider again next month answers a different question than the one already decided.
     * Payments held for a human — the ops queue.
     */
    async listCbxWalletPaymentReviewRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/cbx/wallet-payments/review`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * Every row here is money taken and goods not shipped, which is not a state to leave a buyer in without it appearing on a list. `riskLevel` and `riskExposures` are the verdict as recorded at the time, not re-derived — asking a provider again next month answers a different question than the one already decided.
     * Payments held for a human — the ops queue.
     */
    async listCbxWalletPaymentReview(initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listCbxWalletPaymentReviewRaw(initOverrides);
        return await response.value();
    }

}
