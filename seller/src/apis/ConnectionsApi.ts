/* tslint:disable */
/* eslint-disable */
/**
 * Crossly Seller API
 * The seller-facing Crossly API — listings, crossposting, orders, analytics. Authenticate with a Personal Access Token (`Authorization: Bearer crossly_pat_…`) or a seller OAuth token (`crossly_oat_…`), scoped `<domain>:<read|write>`. Buyer endpoints are a SEPARATE API with a separate principal — see the Crossly Buyer API.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import {
    GetConnectionHealthResponse,
    GetConnectionHealthResponseFromJSON,
    GetConnectionHealthResponseToJSON,
    V1List,
    V1ListFromJSON,
    V1ListToJSON,
} from '../models';

export interface GetConnectionHealthRequest {
    includeUnconnected?: boolean;
}

/**
 * 
 */
export class ConnectionsApi extends runtime.BaseAPI {

    /**
     * One entry per ACCOUNT, not per platform — a healthy slot 1 must not speak for a dead slot 2. States that were never measured are reported as \"unknown\" rather than as failures.
     * Health of each connected marketplace account, with a plain-English diagnosis.
     */
    async getConnectionHealthRaw(requestParameters: GetConnectionHealthRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetConnectionHealthResponse>> {
        const queryParameters: any = {};

        if (requestParameters.includeUnconnected !== undefined) {
            queryParameters['includeUnconnected'] = requestParameters.includeUnconnected;
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/connection-health`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetConnectionHealthResponseFromJSON(jsonValue));
    }

    /**
     * One entry per ACCOUNT, not per platform — a healthy slot 1 must not speak for a dead slot 2. States that were never measured are reported as \"unknown\" rather than as failures.
     * Health of each connected marketplace account, with a plain-English diagnosis.
     */
    async getConnectionHealth(requestParameters: GetConnectionHealthRequest = {}, initOverrides?: RequestInit): Promise<GetConnectionHealthResponse> {
        const response = await this.getConnectionHealthRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Capabilities are what the machine DECLARED it can do at pairing — print, scan_watch, browser, cookie_jar, proxy_bind, scale. A machine only declares \"scale\" when one actually answered, never on the assumption that one might.
     * Machines paired to this account, and what each can do.
     */
    async listDevicesRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/devices`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * Capabilities are what the machine DECLARED it can do at pairing — print, scan_watch, browser, cookie_jar, proxy_bind, scale. A machine only declares \"scale\" when one actually answered, never on the assumption that one might.
     * Machines paired to this account, and what each can do.
     */
    async listDevices(initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listDevicesRaw(initOverrides);
        return await response.value();
    }

}
