/* tslint:disable */
/* eslint-disable */
/**
 * Crossly Seller API
 * The seller-facing Crossly API — listings, crossposting, orders, analytics. Authenticate with a Personal Access Token (`Authorization: Bearer crossly_pat_…`) or a seller OAuth token (`crossly_oat_…`), scoped `<domain>:<read|write>`. Buyer endpoints are a SEPARATE API with a separate principal — see the Crossly Buyer API.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import {
    CreateInventoryBulkArchiveResponse,
    CreateInventoryBulkArchiveResponseFromJSON,
    CreateInventoryBulkArchiveResponseToJSON,
    CreateInventoryBulkDeleteResponse,
    CreateInventoryBulkDeleteResponseFromJSON,
    CreateInventoryBulkDeleteResponseToJSON,
    CreateInventoryBulkLabelResponse,
    CreateInventoryBulkLabelResponseFromJSON,
    CreateInventoryBulkLabelResponseToJSON,
    CreateInventoryBulkQuantityResponse,
    CreateInventoryBulkQuantityResponseFromJSON,
    CreateInventoryBulkQuantityResponseToJSON,
    CreateInventoryCsvImportResponse,
    CreateInventoryCsvImportResponseFromJSON,
    CreateInventoryCsvImportResponseToJSON,
    CreateInventoryLabelRenameResponse,
    CreateInventoryLabelRenameResponseFromJSON,
    CreateInventoryLabelRenameResponseToJSON,
    CreateInventoryResponse,
    CreateInventoryResponseFromJSON,
    CreateInventoryResponseToJSON,
    CreateInventoryUnitIdentifierResponse,
    CreateInventoryUnitIdentifierResponseFromJSON,
    CreateInventoryUnitIdentifierResponseToJSON,
    CreateInventoryUnitLookupResponse,
    CreateInventoryUnitLookupResponseFromJSON,
    CreateInventoryUnitLookupResponseToJSON,
    DeleteInventoryResponse,
    DeleteInventoryResponseFromJSON,
    DeleteInventoryResponseToJSON,
    GetInventoryFacetResponse,
    GetInventoryFacetResponseFromJSON,
    GetInventoryFacetResponseToJSON,
    GetInventoryLabelResponse,
    GetInventoryLabelResponseFromJSON,
    GetInventoryLabelResponseToJSON,
    GetInventoryLabelStatResponse,
    GetInventoryLabelStatResponseFromJSON,
    GetInventoryLabelStatResponseToJSON,
    GetInventoryResponse,
    GetInventoryResponseFromJSON,
    GetInventoryResponseToJSON,
    GetInventorySkuExistResponse,
    GetInventorySkuExistResponseFromJSON,
    GetInventorySkuExistResponseToJSON,
    GetSpatialPublicResponse,
    GetSpatialPublicResponseFromJSON,
    GetSpatialPublicResponseToJSON,
    GetSpatialSceneResponse,
    GetSpatialSceneResponseFromJSON,
    GetSpatialSceneResponseToJSON,
    UpdateInventoryResponse,
    UpdateInventoryResponseFromJSON,
    UpdateInventoryResponseToJSON,
    V1List,
    V1ListFromJSON,
    V1ListToJSON,
} from '../models';

export interface CreateInventoryUnitIdentifierRequest {
    id: string;
}

export interface DeleteInventoryRequest {
    id: string;
}

export interface GetInventoryRequest {
    id: string;
}

export interface GetInventorySkuExistRequest {
    sku: string;
}

export interface GetSpatialPublicRequest {
    slug: string;
}

export interface GetSpatialSceneRequest {
    id: string;
}

export interface ListInventoryRequest {
    page?: number;
    limit?: number;
    search?: string;
    status?: string;
}

export interface ListInventoryActivityRequest {
    id: string;
    limit?: number;
}

export interface ListInventoryUnitsRequest {
    id: string;
}

export interface ListSpatialPublicOffersRequest {
    slug: string;
}

export interface ListSpatialSceneMovementsRequest {
    id: string;
}

export interface UpdateInventoryRequest {
    id: string;
}

/**
 * 
 */
export class InventoryApi extends runtime.BaseAPI {

    /**
     * Create a new inventory item.
     */
    async createInventoryRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateInventoryResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateInventoryResponseFromJSON(jsonValue));
    }

    /**
     * Create a new inventory item.
     */
    async createInventory(initOverrides?: RequestInit): Promise<CreateInventoryResponse> {
        const response = await this.createInventoryRaw(initOverrides);
        return await response.value();
    }

    /**
     * Bulk archive inventory items (soft).
     */
    async createInventoryBulkArchiveRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateInventoryBulkArchiveResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/bulk-archive`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateInventoryBulkArchiveResponseFromJSON(jsonValue));
    }

    /**
     * Bulk archive inventory items (soft).
     */
    async createInventoryBulkArchive(initOverrides?: RequestInit): Promise<CreateInventoryBulkArchiveResponse> {
        const response = await this.createInventoryBulkArchiveRaw(initOverrides);
        return await response.value();
    }

    /**
     * Bulk delete inventory items (delinks listings).
     */
    async createInventoryBulkDeleteRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateInventoryBulkDeleteResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/bulk-delete`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateInventoryBulkDeleteResponseFromJSON(jsonValue));
    }

    /**
     * Bulk delete inventory items (delinks listings).
     */
    async createInventoryBulkDelete(initOverrides?: RequestInit): Promise<CreateInventoryBulkDeleteResponse> {
        const response = await this.createInventoryBulkDeleteRaw(initOverrides);
        return await response.value();
    }

    /**
     * Bulk add/remove labels on inventory items.
     */
    async createInventoryBulkLabelRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateInventoryBulkLabelResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/bulk-labels`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateInventoryBulkLabelResponseFromJSON(jsonValue));
    }

    /**
     * Bulk add/remove labels on inventory items.
     */
    async createInventoryBulkLabel(initOverrides?: RequestInit): Promise<CreateInventoryBulkLabelResponse> {
        const response = await this.createInventoryBulkLabelRaw(initOverrides);
        return await response.value();
    }

    /**
     * Set / add / subtract stock across many items, syncing live listings.
     */
    async createInventoryBulkQuantityRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateInventoryBulkQuantityResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/bulk-quantity`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateInventoryBulkQuantityResponseFromJSON(jsonValue));
    }

    /**
     * Set / add / subtract stock across many items, syncing live listings.
     */
    async createInventoryBulkQuantity(initOverrides?: RequestInit): Promise<CreateInventoryBulkQuantityResponse> {
        const response = await this.createInventoryBulkQuantityRaw(initOverrides);
        return await response.value();
    }

    /**
     * Export inventory as CSV. Round-trips back through csv/import.
     */
    async createInventoryCsvExportRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<string>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/csv/export`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.TextApiResponse(response) as any;
    }

    /**
     * Export inventory as CSV. Round-trips back through csv/import.
     */
    async createInventoryCsvExport(initOverrides?: RequestInit): Promise<string> {
        const response = await this.createInventoryCsvExportRaw(initOverrides);
        return await response.value();
    }

    /**
     * Import a CSV. Rows whose sku matches an existing item update it; others are added. Pass dryRun to preview.
     */
    async createInventoryCsvImportRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateInventoryCsvImportResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/csv/import`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateInventoryCsvImportResponseFromJSON(jsonValue));
    }

    /**
     * Import a CSV. Rows whose sku matches an existing item update it; others are added. Pass dryRun to preview.
     */
    async createInventoryCsvImport(initOverrides?: RequestInit): Promise<CreateInventoryCsvImportResponse> {
        const response = await this.createInventoryCsvImportRaw(initOverrides);
        return await response.value();
    }

    /**
     * Rename a label across every inventory item.
     */
    async createInventoryLabelRenameRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateInventoryLabelRenameResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/labels/rename`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateInventoryLabelRenameResponseFromJSON(jsonValue));
    }

    /**
     * Rename a label across every inventory item.
     */
    async createInventoryLabelRename(initOverrides?: RequestInit): Promise<CreateInventoryLabelRenameResponse> {
        const response = await this.createInventoryLabelRenameRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates the unit lazily if no `unitId` is given. Recording BEFORE the item sells is what makes the identifier usable as evidence on a return — one first recorded after a dispute opens is graded `weak` and says so.
     * Record a serial, IMEI, or licence key against an inventory item.
     */
    async createInventoryUnitIdentifierRaw(requestParameters: CreateInventoryUnitIdentifierRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateInventoryUnitIdentifierResponse>> {
        if (requestParameters.id === null || requestParameters.id === undefined) {
            throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling createInventoryUnitIdentifier.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/{id}/units/identifiers`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateInventoryUnitIdentifierResponseFromJSON(jsonValue));
    }

    /**
     * Creates the unit lazily if no `unitId` is given. Recording BEFORE the item sells is what makes the identifier usable as evidence on a return — one first recorded after a dispute opens is graded `weak` and says so.
     * Record a serial, IMEI, or licence key against an inventory item.
     */
    async createInventoryUnitIdentifier(requestParameters: CreateInventoryUnitIdentifierRequest, initOverrides?: RequestInit): Promise<CreateInventoryUnitIdentifierResponse> {
        const response = await this.createInventoryUnitIdentifierRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * \"Have I ever seen this serial?\" — for when something arrives back and nobody knows which order it belongs to. Scoped to the caller, so it can never be used to probe another seller\'s stock.
     * Find a unit by identifier.
     */
    async createInventoryUnitLookupRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateInventoryUnitLookupResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/units/lookup`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateInventoryUnitLookupResponseFromJSON(jsonValue));
    }

    /**
     * \"Have I ever seen this serial?\" — for when something arrives back and nobody knows which order it belongs to. Scoped to the caller, so it can never be used to probe another seller\'s stock.
     * Find a unit by identifier.
     */
    async createInventoryUnitLookup(initOverrides?: RequestInit): Promise<CreateInventoryUnitLookupResponse> {
        const response = await this.createInventoryUnitLookupRaw(initOverrides);
        return await response.value();
    }

    /**
     * Soft-archive an inventory item.
     */
    async deleteInventoryRaw(requestParameters: DeleteInventoryRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<DeleteInventoryResponse>> {
        if (requestParameters.id === null || requestParameters.id === undefined) {
            throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling deleteInventory.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DeleteInventoryResponseFromJSON(jsonValue));
    }

    /**
     * Soft-archive an inventory item.
     */
    async deleteInventory(requestParameters: DeleteInventoryRequest, initOverrides?: RequestInit): Promise<DeleteInventoryResponse> {
        const response = await this.deleteInventoryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Get one inventory item with platform listings.
     */
    async getInventoryRaw(requestParameters: GetInventoryRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetInventoryResponse>> {
        if (requestParameters.id === null || requestParameters.id === undefined) {
            throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getInventory.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetInventoryResponseFromJSON(jsonValue));
    }

    /**
     * Get one inventory item with platform listings.
     */
    async getInventory(requestParameters: GetInventoryRequest, initOverrides?: RequestInit): Promise<GetInventoryResponse> {
        const response = await this.getInventoryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Distinct brands + categories across this user\'s inventory.
     */
    async getInventoryFacetRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetInventoryFacetResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/facets`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetInventoryFacetResponseFromJSON(jsonValue));
    }

    /**
     * Distinct brands + categories across this user\'s inventory.
     */
    async getInventoryFacet(initOverrides?: RequestInit): Promise<GetInventoryFacetResponse> {
        const response = await this.getInventoryFacetRaw(initOverrides);
        return await response.value();
    }

    /**
     * List every distinct label across this user\'s inventory.
     */
    async getInventoryLabelRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetInventoryLabelResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/labels`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetInventoryLabelResponseFromJSON(jsonValue));
    }

    /**
     * List every distinct label across this user\'s inventory.
     */
    async getInventoryLabel(initOverrides?: RequestInit): Promise<GetInventoryLabelResponse> {
        const response = await this.getInventoryLabelRaw(initOverrides);
        return await response.value();
    }

    /**
     * List distinct labels with usage counts + colors.
     */
    async getInventoryLabelStatRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetInventoryLabelStatResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/labels/stats`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetInventoryLabelStatResponseFromJSON(jsonValue));
    }

    /**
     * List distinct labels with usage counts + colors.
     */
    async getInventoryLabelStat(initOverrides?: RequestInit): Promise<GetInventoryLabelStatResponse> {
        const response = await this.getInventoryLabelStatRaw(initOverrides);
        return await response.value();
    }

    /**
     * Check whether a SKU is already in use on this user\'s inventory.
     */
    async getInventorySkuExistRaw(requestParameters: GetInventorySkuExistRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetInventorySkuExistResponse>> {
        if (requestParameters.sku === null || requestParameters.sku === undefined) {
            throw new runtime.RequiredError('sku','Required parameter requestParameters.sku was null or undefined when calling getInventorySkuExist.');
        }

        const queryParameters: any = {};

        if (requestParameters.sku !== undefined) {
            queryParameters['sku'] = requestParameters.sku;
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/sku-exists`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetInventorySkuExistResponseFromJSON(jsonValue));
    }

    /**
     * Check whether a SKU is already in use on this user\'s inventory.
     */
    async getInventorySkuExist(requestParameters: GetInventorySkuExistRequest, initOverrides?: RequestInit): Promise<GetInventorySkuExistResponse> {
        const response = await this.getInventorySkuExistRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * The room behind a share link: container geometry in METRES matching the real physical object, the solved arrangement, and one row per object on the shelves. REDACTED relative to the owner\'s view — no cost, no storage location, no listing status — so do not expect the fields /v1/spatial/scenes/{id} returns. Each placement carries `pinned`: true means a HUMAN put it there and it will not move; false means a layout SOLVER chose, and it may choose differently once the stock changes, so an unpinned placement is never a statement about where something physically is. `solved.overflow` lists what did not fit — a non-empty array means the room is INCOMPLETE and `stats.itemCount` exceeds what is on screen. Resolves rooms shared as `unlisted` as well as `public`: holding the link is the permission.
     * A shared room, as a visitor sees it.
     */
    async getSpatialPublicRaw(requestParameters: GetSpatialPublicRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetSpatialPublicResponse>> {
        if (requestParameters.slug === null || requestParameters.slug === undefined) {
            throw new runtime.RequiredError('slug','Required parameter requestParameters.slug was null or undefined when calling getSpatialPublic.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/spatial/public/{slug}`.replace(`{${"slug"}}`, encodeURIComponent(String(requestParameters.slug))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetSpatialPublicResponseFromJSON(jsonValue));
    }

    /**
     * The room behind a share link: container geometry in METRES matching the real physical object, the solved arrangement, and one row per object on the shelves. REDACTED relative to the owner\'s view — no cost, no storage location, no listing status — so do not expect the fields /v1/spatial/scenes/{id} returns. Each placement carries `pinned`: true means a HUMAN put it there and it will not move; false means a layout SOLVER chose, and it may choose differently once the stock changes, so an unpinned placement is never a statement about where something physically is. `solved.overflow` lists what did not fit — a non-empty array means the room is INCOMPLETE and `stats.itemCount` exceeds what is on screen. Resolves rooms shared as `unlisted` as well as `public`: holding the link is the permission.
     * A shared room, as a visitor sees it.
     */
    async getSpatialPublic(requestParameters: GetSpatialPublicRequest, initOverrides?: RequestInit): Promise<GetSpatialPublicResponse> {
        const response = await this.getSpatialPublicRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the space profile (container geometry in METRES, matching the real physical object), the solved placements, and the items. Placements carry a `pinned` flag: true means a human put it there and the layout solver will not move it; false means the solver chose, and it may choose differently once the stock changes. `overflow` lists anything that did not fit — it is reported, never silently dropped.
     * A solved room: every item, where it sits, and why.
     */
    async getSpatialSceneRaw(requestParameters: GetSpatialSceneRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetSpatialSceneResponse>> {
        if (requestParameters.id === null || requestParameters.id === undefined) {
            throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getSpatialScene.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/spatial/scenes/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetSpatialSceneResponseFromJSON(jsonValue));
    }

    /**
     * Returns the space profile (container geometry in METRES, matching the real physical object), the solved placements, and the items. Placements carry a `pinned` flag: true means a human put it there and the layout solver will not move it; false means the solver chose, and it may choose differently once the stock changes. `overflow` lists anything that did not fit — it is reported, never silently dropped.
     * A solved room: every item, where it sits, and why.
     */
    async getSpatialScene(requestParameters: GetSpatialSceneRequest, initOverrides?: RequestInit): Promise<GetSpatialSceneResponse> {
        const response = await this.getSpatialSceneRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * List inventory items.
     */
    async listInventoryRaw(requestParameters: ListInventoryRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        const queryParameters: any = {};

        if (requestParameters.page !== undefined) {
            queryParameters['page'] = requestParameters.page;
        }

        if (requestParameters.limit !== undefined) {
            queryParameters['limit'] = requestParameters.limit;
        }

        if (requestParameters.search !== undefined) {
            queryParameters['search'] = requestParameters.search;
        }

        if (requestParameters.status !== undefined) {
            queryParameters['status'] = requestParameters.status;
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * List inventory items.
     */
    async listInventory(requestParameters: ListInventoryRequest = {}, initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listInventoryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Activity log for an inventory item (created/sold/edited/etc.).
     */
    async listInventoryActivityRaw(requestParameters: ListInventoryActivityRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        if (requestParameters.id === null || requestParameters.id === undefined) {
            throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling listInventoryActivity.');
        }

        const queryParameters: any = {};

        if (requestParameters.limit !== undefined) {
            queryParameters['limit'] = requestParameters.limit;
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/{id}/activity`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * Activity log for an inventory item (created/sold/edited/etc.).
     */
    async listInventoryActivity(requestParameters: ListInventoryActivityRequest, initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listInventoryActivityRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Filter inventory → return matching id list.
     */
    async listInventoryIdsRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/ids`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * Filter inventory → return matching id list.
     */
    async listInventoryIds(initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listInventoryIdsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Each identifier carries a `strength` describing what it proves: `strong` was recorded before the item sold, `good` at packing, `weak` only after it shipped. The grade is derived from when it was recorded, never from the value itself.
     * List the individually identified units of an inventory item.
     */
    async listInventoryUnitsRaw(requestParameters: ListInventoryUnitsRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        if (requestParameters.id === null || requestParameters.id === undefined) {
            throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling listInventoryUnits.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/{id}/units`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * Each identifier carries a `strength` describing what it proves: `strong` was recorded before the item sold, `good` at packing, `weak` only after it shipped. The grade is derived from when it was recorded, never from the value itself.
     * List the individually identified units of an inventory item.
     */
    async listInventoryUnits(requestParameters: ListInventoryUnitsRequest, initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listInventoryUnitsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * The directory behind world-hopping. A room becomes public when its owner shares it; this lists those, newest first, with enough to draw a doorway AND enough to choose one — name, slug, category, itemCount, up to four previewImages, forSaleCount, a priceFromCents/priceToCents band and updatedAt. The band is the cheapest and dearest thing for sale in the room, never a quote for one object: /api/public/spatial/{slug}/offers is the authority on that. Fetch the room itself from /api/public/spatial/{slug}. Rooms with no items are omitted: an empty room is not a destination.
     * Public rooms anyone can walk into.
     */
    async listSpatialPublicRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/spatial/public`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * The directory behind world-hopping. A room becomes public when its owner shares it; this lists those, newest first, with enough to draw a doorway AND enough to choose one — name, slug, category, itemCount, up to four previewImages, forSaleCount, a priceFromCents/priceToCents band and updatedAt. The band is the cheapest and dearest thing for sale in the room, never a quote for one object: /api/public/spatial/{slug}/offers is the authority on that. Fetch the room itself from /api/public/spatial/{slug}. Rooms with no items are omitted: an empty room is not a destination.
     * Public rooms anyone can walk into.
     */
    async listSpatialPublic(initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listSpatialPublicRaw(initOverrides);
        return await response.value();
    }

    /**
     * Price, stock, condition and grade for everything in the room its owner is actually selling. Correlate to the room by `itemId`, which is the SAME id the scene payload publishes per item — never by title. An item in the room with no row here is not for sale; an empty array means the owner is showing the collection rather than selling it, which is a different answer from a 404 (no such shared room). SEPARATE CALL ON PURPOSE: the room\'s geometry is stable for minutes, a price is not — it changes whenever the seller edits a listing. Re-read this before quoting, and do not cache a price alongside a cached room. `priceCents` is CENTS. `available` is remaining stock, or null when the listing declares none; null is unknown, not zero. Where an item sits inside more than one active listing, the offer quoted is the one for that item alone rather than a bundle it belongs to.
     * What is for sale in a shared room.
     */
    async listSpatialPublicOffersRaw(requestParameters: ListSpatialPublicOffersRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        if (requestParameters.slug === null || requestParameters.slug === undefined) {
            throw new runtime.RequiredError('slug','Required parameter requestParameters.slug was null or undefined when calling listSpatialPublicOffers.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/spatial/public/{slug}/offers`.replace(`{${"slug"}}`, encodeURIComponent(String(requestParameters.slug))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * Price, stock, condition and grade for everything in the room its owner is actually selling. Correlate to the room by `itemId`, which is the SAME id the scene payload publishes per item — never by title. An item in the room with no row here is not for sale; an empty array means the owner is showing the collection rather than selling it, which is a different answer from a 404 (no such shared room). SEPARATE CALL ON PURPOSE: the room\'s geometry is stable for minutes, a price is not — it changes whenever the seller edits a listing. Re-read this before quoting, and do not cache a price alongside a cached room. `priceCents` is CENTS. `available` is remaining stock, or null when the listing declares none; null is unknown, not zero. Where an item sits inside more than one active listing, the offer quoted is the one for that item alone rather than a bundle it belongs to.
     * What is for sale in a shared room.
     */
    async listSpatialPublicOffers(requestParameters: ListSpatialPublicOffersRequest, initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listSpatialPublicOffersRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * One row per physical transition: which item, from which node, to which node, when, and of what kind (placed/moved/picked/shipped/received/removed). Nodes are referenced by id; the `fromCode`/`toCode` strings are display snapshots of the location code AT THE TIME and are not stable identifiers — correlate on the node ids. `since`/`until` are ISO timestamps, defaulting to the last seven days and clamped to 90. Movements are NOT attributed to individual team members on this surface: a token has no team role, so there is no honest way to decide whether its holder may see who did the work.
     * Stock movements in a room over a time window.
     */
    async listSpatialSceneMovementsRaw(requestParameters: ListSpatialSceneMovementsRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        if (requestParameters.id === null || requestParameters.id === undefined) {
            throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling listSpatialSceneMovements.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/spatial/scenes/{id}/movements`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * One row per physical transition: which item, from which node, to which node, when, and of what kind (placed/moved/picked/shipped/received/removed). Nodes are referenced by id; the `fromCode`/`toCode` strings are display snapshots of the location code AT THE TIME and are not stable identifiers — correlate on the node ids. `since`/`until` are ISO timestamps, defaulting to the last seven days and clamped to 90. Movements are NOT attributed to individual team members on this surface: a token has no team role, so there is no honest way to decide whether its holder may see who did the work.
     * Stock movements in a room over a time window.
     */
    async listSpatialSceneMovements(requestParameters: ListSpatialSceneMovementsRequest, initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listSpatialSceneMovementsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * One per market category the seller holds catalog-resolved stock in, plus a warehouse. Rooms are created on first read rather than requiring setup, so this call is safe to treat as the entry point.
     * The rooms this account has.
     */
    async listSpatialScenesRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/spatial/scenes`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * One per market category the seller holds catalog-resolved stock in, plus a warehouse. Rooms are created on first read rather than requiring setup, so this call is safe to treat as the entry point.
     * The rooms this account has.
     */
    async listSpatialScenes(initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listSpatialScenesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Update an inventory item (partial).
     */
    async updateInventoryRaw(requestParameters: UpdateInventoryRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<UpdateInventoryResponse>> {
        if (requestParameters.id === null || requestParameters.id === undefined) {
            throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling updateInventory.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("PersonalAccessToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UpdateInventoryResponseFromJSON(jsonValue));
    }

    /**
     * Update an inventory item (partial).
     */
    async updateInventory(requestParameters: UpdateInventoryRequest, initOverrides?: RequestInit): Promise<UpdateInventoryResponse> {
        const response = await this.updateInventoryRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
