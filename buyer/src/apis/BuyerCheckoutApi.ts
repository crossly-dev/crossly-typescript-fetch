/* tslint:disable */
/* eslint-disable */
/**
 * Crossly Buyer API
 * The buyer-facing Crossly API — cart, orders, wishlists, offers, cashback. A DIFFERENT principal from the seller API: authenticated by a buyer OAuth token scoped `buyer:*` and resolved by `resolveBuyer`. A seller Personal Access Token will NOT authenticate these endpoints.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import {
    CreateBuyerCheckoutResponse,
    CreateBuyerCheckoutResponseFromJSON,
    CreateBuyerCheckoutResponseToJSON,
    GetBuyerCheckoutControlResponse,
    GetBuyerCheckoutControlResponseFromJSON,
    GetBuyerCheckoutControlResponseToJSON,
    UpdateBuyerCheckoutControlResponse,
    UpdateBuyerCheckoutControlResponseFromJSON,
    UpdateBuyerCheckoutControlResponseToJSON,
} from '../models';

/**
 * 
 */
export class BuyerCheckoutApi extends runtime.BaseAPI {

    /**
     * An Idempotency-Key header is REQUIRED — this endpoint refuses without one, because a retried request would otherwise buy the item twice and a retry is the most likely thing an automated buyer does. Derive the key from what you are buying and reuse it across retries; a fresh random value per attempt satisfies the check and keeps the bug. The item is QUOTED first and the delivered total is checked against both your maxTotalCents and this key\'s limits before anything is charged. A card that demands 3-D Secure cannot be charged unattended; that answers 402 with `authentication_required` and the purchase must be finished on Crossly.
     * Buy a listing without being present.
     */
    async createBuyerCheckoutRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateBuyerCheckoutResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BuyerOAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/buyer/checkout`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateBuyerCheckoutResponseFromJSON(jsonValue));
    }

    /**
     * An Idempotency-Key header is REQUIRED — this endpoint refuses without one, because a retried request would otherwise buy the item twice and a retry is the most likely thing an automated buyer does. Derive the key from what you are buying and reuse it across retries; a fresh random value per attempt satisfies the check and keeps the bug. The item is QUOTED first and the delivered total is checked against both your maxTotalCents and this key\'s limits before anything is charged. A card that demands 3-D Secure cannot be charged unattended; that answers 402 with `authentication_required` and the purchase must be finished on Crossly.
     * Buy a listing without being present.
     */
    async createBuyerCheckout(initOverrides?: RequestInit): Promise<CreateBuyerCheckoutResponse> {
        const response = await this.createBuyerCheckoutRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reports the controls for the key making the call — not for your account. Every key has its own switch and its own limits.
     * What this key is allowed to spend.
     */
    async getBuyerCheckoutControlRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetBuyerCheckoutControlResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BuyerOAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/buyer/checkout/controls`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetBuyerCheckoutControlResponseFromJSON(jsonValue));
    }

    /**
     * Reports the controls for the key making the call — not for your account. Every key has its own switch and its own limits.
     * What this key is allowed to spend.
     */
    async getBuyerCheckoutControl(initOverrides?: RequestInit): Promise<GetBuyerCheckoutControlResponse> {
        const response = await this.getBuyerCheckoutControlRaw(initOverrides);
        return await response.value();
    }

    /**
     * A key can only ever raise or lower ITS OWN limits, and only if the token already carries buyer:checkout:write. Turning it off takes effect immediately.
     * Switch this key on for spending, and set its limits.
     */
    async updateBuyerCheckoutControlRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<UpdateBuyerCheckoutControlResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BuyerOAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/buyer/checkout/controls`,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UpdateBuyerCheckoutControlResponseFromJSON(jsonValue));
    }

    /**
     * A key can only ever raise or lower ITS OWN limits, and only if the token already carries buyer:checkout:write. Turning it off takes effect immediately.
     * Switch this key on for spending, and set its limits.
     */
    async updateBuyerCheckoutControl(initOverrides?: RequestInit): Promise<UpdateBuyerCheckoutControlResponse> {
        const response = await this.updateBuyerCheckoutControlRaw(initOverrides);
        return await response.value();
    }

}
