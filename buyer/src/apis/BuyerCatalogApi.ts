/* tslint:disable */
/* eslint-disable */
/**
 * Crossly Buyer API
 * The buyer-facing Crossly API — cart, orders, wishlists, offers, cashback. A DIFFERENT principal from the seller API: authenticated by a buyer OAuth token scoped `buyer:*` and resolved by `resolveBuyer`. A seller Personal Access Token will NOT authenticate these endpoints.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import {
    CreateBuyerIdentifyResponse,
    CreateBuyerIdentifyResponseFromJSON,
    CreateBuyerIdentifyResponseToJSON,
    CreateBuyerLockonConfirmResponse,
    CreateBuyerLockonConfirmResponseFromJSON,
    CreateBuyerLockonConfirmResponseToJSON,
    CreateBuyerLockonObserveResponse,
    CreateBuyerLockonObserveResponseFromJSON,
    CreateBuyerLockonObserveResponseToJSON,
    CreateBuyerLockonResponse,
    CreateBuyerLockonResponseFromJSON,
    CreateBuyerLockonResponseToJSON,
    CreateBuyerScanResponse,
    CreateBuyerScanResponseFromJSON,
    CreateBuyerScanResponseToJSON,
    CreateBuyerScanSessionEndResponse,
    CreateBuyerScanSessionEndResponseFromJSON,
    CreateBuyerScanSessionEndResponseToJSON,
    CreateBuyerScanSessionResponse,
    CreateBuyerScanSessionResponseFromJSON,
    CreateBuyerScanSessionResponseToJSON,
    GetBuyerAnywhereResponse,
    GetBuyerAnywhereResponseFromJSON,
    GetBuyerAnywhereResponseToJSON,
    GetBuyerCatalogFacetResponse,
    GetBuyerCatalogFacetResponseFromJSON,
    GetBuyerCatalogFacetResponseToJSON,
    GetBuyerCatalogListingAvailabilityResponse,
    GetBuyerCatalogListingAvailabilityResponseFromJSON,
    GetBuyerCatalogListingAvailabilityResponseToJSON,
    GetBuyerCatalogListingResponse,
    GetBuyerCatalogListingResponseFromJSON,
    GetBuyerCatalogListingResponseToJSON,
    GetBuyerScanSessionResponse,
    GetBuyerScanSessionResponseFromJSON,
    GetBuyerScanSessionResponseToJSON,
    V1List,
    V1ListFromJSON,
    V1ListToJSON,
} from '../models';

export interface CreateBuyerLockonConfirmRequest {
    id: string;
}

export interface CreateBuyerLockonObserveRequest {
    id: string;
}

export interface CreateBuyerScanSessionEndRequest {
    id: string;
}

export interface GetBuyerCatalogListingRequest {
    slug: string;
}

export interface GetBuyerCatalogListingAvailabilityRequest {
    slug: string;
}

export interface GetBuyerScanSessionRequest {
    id: string;
}

/**
 * 
 */
export class BuyerCatalogApi extends runtime.BaseAPI {

    /**
     * The gesture endpoint for Live Shop. Runs a cost ladder: a decoded BARCODE resolves in ~50ms for nothing; failing that, self-hosted CLIP matches the catalogue; failing that, a vision model names it (the only rung that costs anything, capped per buyer per day). `hud` is pre-formatted for a 600×600 lens — one headline, one subline, up to three fact chips and exactly ONE action, because a pinch cannot choose between buttons. A vision label is WORDS, never an identity: it names the thing so the buyer can search, and never drives a price comparison.
     * Identify a held object and return a HUD-ready answer.
     */
    async createBuyerIdentifyRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateBuyerIdentifyResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BuyerOAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/buyer/identify`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateBuyerIdentifyResponseFromJSON(jsonValue));
    }

    /**
     * The gesture endpoint for Live Shop. Runs a cost ladder: a decoded BARCODE resolves in ~50ms for nothing; failing that, self-hosted CLIP matches the catalogue; failing that, a vision model names it (the only rung that costs anything, capped per buyer per day). `hud` is pre-formatted for a 600×600 lens — one headline, one subline, up to three fact chips and exactly ONE action, because a pinch cannot choose between buttons. A vision label is WORDS, never an identity: it names the thing so the buyer can search, and never drives a price comparison.
     * Identify a held object and return a HUD-ready answer.
     */
    async createBuyerIdentify(initOverrides?: RequestInit): Promise<CreateBuyerIdentifyResponse> {
        const response = await this.createBuyerIdentifyRaw(initOverrides);
        return await response.value();
    }

    /**
     * Open this when on-device tracking acquires an object, then post observations to it as the buyer turns the thing over. The answer improves as evidence arrives — the style code inside a shoe settles what the front of it could not.
     * Lock on to an object the buyer is holding.
     */
    async createBuyerLockonRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateBuyerLockonResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BuyerOAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/buyer/lockons`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateBuyerLockonResponseFromJSON(jsonValue));
    }

    /**
     * Open this when on-device tracking acquires an object, then post observations to it as the buyer turns the thing over. The answer improves as evidence arrives — the style code inside a shoe settles what the front of it could not.
     * Lock on to an object the buyer is holding.
     */
    async createBuyerLockon(initOverrides?: RequestInit): Promise<CreateBuyerLockonResponse> {
        const response = await this.createBuyerLockonRaw(initOverrides);
        return await response.value();
    }

    /**
     * Promotes a text match to a CONFIRMED identity — the strongest evidence in the system, because a person holding the object said yes. Validated against the candidates we actually offered, so it cannot be claimed about an arbitrary product.
     * The buyer picked one of the candidates.
     */
    async createBuyerLockonConfirmRaw(requestParameters: CreateBuyerLockonConfirmRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateBuyerLockonConfirmResponse>> {
        if (requestParameters.id === null || requestParameters.id === undefined) {
            throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling createBuyerLockonConfirm.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BuyerOAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/buyer/lockons/{id}/confirm`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateBuyerLockonConfirmResponseFromJSON(jsonValue));
    }

    /**
     * Promotes a text match to a CONFIRMED identity — the strongest evidence in the system, because a person holding the object said yes. Validated against the candidates we actually offered, so it cannot be claimed about an arbitrary product.
     * The buyer picked one of the candidates.
     */
    async createBuyerLockonConfirm(requestParameters: CreateBuyerLockonConfirmRequest, initOverrides?: RequestInit): Promise<CreateBuyerLockonConfirmResponse> {
        const response = await this.createBuyerLockonConfirmRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Send only what you LEARNED: a decoded barcode, newly-read OCR text, or a frame when neither settled it. Do not post every frame — tracking and decoding happen on-device for free, and this endpoint is for evidence, not video. Evidence is RANKED (confirmed > barcode > ocr > visual), so a late weak reading can never overwrite a strong early one. When text evidence finds several products, `candidates` comes back for the buyer to pick from — a vision label is words, and only a human confirmation turns it into an identity we will price against.
     * Add what this frame revealed, and get the current best answer.
     */
    async createBuyerLockonObserveRaw(requestParameters: CreateBuyerLockonObserveRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateBuyerLockonObserveResponse>> {
        if (requestParameters.id === null || requestParameters.id === undefined) {
            throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling createBuyerLockonObserve.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BuyerOAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/buyer/lockons/{id}/observe`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateBuyerLockonObserveResponseFromJSON(jsonValue));
    }

    /**
     * Send only what you LEARNED: a decoded barcode, newly-read OCR text, or a frame when neither settled it. Do not post every frame — tracking and decoding happen on-device for free, and this endpoint is for evidence, not video. Evidence is RANKED (confirmed > barcode > ocr > visual), so a late weak reading can never overwrite a strong early one. When text evidence finds several products, `candidates` comes back for the buyer to pick from — a vision label is words, and only a human confirmation turns it into an identity we will price against.
     * Add what this frame revealed, and get the current best answer.
     */
    async createBuyerLockonObserve(requestParameters: CreateBuyerLockonObserveRequest, initOverrides?: RequestInit): Promise<CreateBuyerLockonObserveResponse> {
        const response = await this.createBuyerLockonObserveRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Send a barcode identifier OR a photo. A BARCODE establishes identity, so the response carries a full price verdict across Crossly and other retailers. A PHOTO establishes resemblance only: you get visual matches from the Crossly catalogue, and a price verdict ONLY if the matched listing carries a real identifier. When it does not, `comparable` is false and there is no verdict — a price comparison built on a visual guess is a claim about a different product. Most second-hand items have no identifier by nature, so this is expected rather than a failure.
     * Identify a physical item and find the cheapest place to buy it.
     */
    async createBuyerScanRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateBuyerScanResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BuyerOAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/buyer/scan`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateBuyerScanResponseFromJSON(jsonValue));
    }

    /**
     * Send a barcode identifier OR a photo. A BARCODE establishes identity, so the response carries a full price verdict across Crossly and other retailers. A PHOTO establishes resemblance only: you get visual matches from the Crossly catalogue, and a price verdict ONLY if the matched listing carries a real identifier. When it does not, `comparable` is false and there is no verdict — a price comparison built on a visual guess is a claim about a different product. Most second-hand items have no identifier by nature, so this is expected rather than a failure.
     * Identify a physical item and find the cheapest place to buy it.
     */
    async createBuyerScan(initOverrides?: RequestInit): Promise<CreateBuyerScanResponse> {
        const response = await this.createBuyerScanRaw(initOverrides);
        return await response.value();
    }

    /**
     * Call this when the glasses connect, then pass the returned id as `sessionId` on each scan. Opening a session CLOSES any other live one — a person is in one shop at a time, and two live sessions split a trip across both.
     * Open a Live Shop session.
     */
    async createBuyerScanSessionRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateBuyerScanSessionResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BuyerOAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/buyer/scan/sessions`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateBuyerScanSessionResponseFromJSON(jsonValue));
    }

    /**
     * Call this when the glasses connect, then pass the returned id as `sessionId` on each scan. Opening a session CLOSES any other live one — a person is in one shop at a time, and two live sessions split a trip across both.
     * Open a Live Shop session.
     */
    async createBuyerScanSession(initOverrides?: RequestInit): Promise<CreateBuyerScanSessionResponse> {
        const response = await this.createBuyerScanSessionRaw(initOverrides);
        return await response.value();
    }

    /**
     * Close a Live Shop session.
     */
    async createBuyerScanSessionEndRaw(requestParameters: CreateBuyerScanSessionEndRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<CreateBuyerScanSessionEndResponse>> {
        if (requestParameters.id === null || requestParameters.id === undefined) {
            throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling createBuyerScanSessionEnd.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BuyerOAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/buyer/scan/sessions/{id}/end`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateBuyerScanSessionEndResponseFromJSON(jsonValue));
    }

    /**
     * Close a Live Shop session.
     */
    async createBuyerScanSessionEnd(requestParameters: CreateBuyerScanSessionEndRequest, initOverrides?: RequestInit): Promise<CreateBuyerScanSessionEndResponse> {
        const response = await this.createBuyerScanSessionEndRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Answers with a VERDICT, not a list: crossly_best, offsite_cheaper, offsite_only or no_match. Offsite offers come from licensed affiliate product feeds, are ranked CHEAPEST-FIRST — commission only ever breaks a sub-$1 tie — and only appear when they beat the price you passed in. `shippingUnknown: true` means a compared price omitted postage, so present the result as \"before postage\" rather than as a delivered total. Crossly wins ties within $1; beyond that the honest answer wins.
     * Cheapest source for an item — Crossly first, then other retailers.
     */
    async getBuyerAnywhereRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetBuyerAnywhereResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BuyerOAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/buyer/anywhere`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetBuyerAnywhereResponseFromJSON(jsonValue));
    }

    /**
     * Answers with a VERDICT, not a list: crossly_best, offsite_cheaper, offsite_only or no_match. Offsite offers come from licensed affiliate product feeds, are ranked CHEAPEST-FIRST — commission only ever breaks a sub-$1 tie — and only appear when they beat the price you passed in. `shippingUnknown: true` means a compared price omitted postage, so present the result as \"before postage\" rather than as a delivered total. Crossly wins ties within $1; beyond that the honest answer wins.
     * Cheapest source for an item — Crossly first, then other retailers.
     */
    async getBuyerAnywhere(initOverrides?: RequestInit): Promise<GetBuyerAnywhereResponse> {
        const response = await this.getBuyerAnywhereRaw(initOverrides);
        return await response.value();
    }

    /**
     * The vocabulary the search filters accept. Counts are live, so a filter built from this will never return an empty page for a value that has since sold out.
     * Brands, categories and conditions that currently have stock.
     */
    async getBuyerCatalogFacetRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetBuyerCatalogFacetResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BuyerOAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/buyer/catalog/facets`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetBuyerCatalogFacetResponseFromJSON(jsonValue));
    }

    /**
     * The vocabulary the search filters accept. Counts are live, so a filter built from this will never return an empty page for a value that has since sold out.
     * Brands, categories and conditions that currently have stock.
     */
    async getBuyerCatalogFacet(initOverrides?: RequestInit): Promise<GetBuyerCatalogFacetResponse> {
        const response = await this.getBuyerCatalogFacetRaw(initOverrides);
        return await response.value();
    }

    /**
     * One listing, in full.
     */
    async getBuyerCatalogListingRaw(requestParameters: GetBuyerCatalogListingRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetBuyerCatalogListingResponse>> {
        if (requestParameters.slug === null || requestParameters.slug === undefined) {
            throw new runtime.RequiredError('slug','Required parameter requestParameters.slug was null or undefined when calling getBuyerCatalogListing.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BuyerOAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/buyer/catalog/listings/{slug}`.replace(`{${"slug"}}`, encodeURIComponent(String(requestParameters.slug))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetBuyerCatalogListingResponseFromJSON(jsonValue));
    }

    /**
     * One listing, in full.
     */
    async getBuyerCatalogListing(requestParameters: GetBuyerCatalogListingRequest, initOverrides?: RequestInit): Promise<GetBuyerCatalogListingResponse> {
        const response = await this.getBuyerCatalogListingRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * The cheapest endpoint here, and the one to poll if you are going to poll — a single indexed row, no joins beyond stock, and an ETag so an unchanged answer is a 304. If you want to be TOLD instead of asking, create a monitor.
     * Is it still buyable, and at what price.
     */
    async getBuyerCatalogListingAvailabilityRaw(requestParameters: GetBuyerCatalogListingAvailabilityRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetBuyerCatalogListingAvailabilityResponse>> {
        if (requestParameters.slug === null || requestParameters.slug === undefined) {
            throw new runtime.RequiredError('slug','Required parameter requestParameters.slug was null or undefined when calling getBuyerCatalogListingAvailability.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BuyerOAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/buyer/catalog/listings/{slug}/availability`.replace(`{${"slug"}}`, encodeURIComponent(String(requestParameters.slug))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetBuyerCatalogListingAvailabilityResponseFromJSON(jsonValue));
    }

    /**
     * The cheapest endpoint here, and the one to poll if you are going to poll — a single indexed row, no joins beyond stock, and an ETag so an unchanged answer is a 304. If you want to be TOLD instead of asking, create a monitor.
     * Is it still buyable, and at what price.
     */
    async getBuyerCatalogListingAvailability(requestParameters: GetBuyerCatalogListingAvailabilityRequest, initOverrides?: RequestInit): Promise<GetBuyerCatalogListingAvailabilityResponse> {
        const response = await this.getBuyerCatalogListingAvailabilityRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Verdicts are returned EXACTLY as they were given at the time, not re-priced. A history screen that silently refreshes old prices shows a saving that was never actually on offer.
     * One trip and everything it found.
     */
    async getBuyerScanSessionRaw(requestParameters: GetBuyerScanSessionRequest, initOverrides?: RequestInit): Promise<runtime.ApiResponse<GetBuyerScanSessionResponse>> {
        if (requestParameters.id === null || requestParameters.id === undefined) {
            throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getBuyerScanSession.');
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BuyerOAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/buyer/scan/sessions/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetBuyerScanSessionResponseFromJSON(jsonValue));
    }

    /**
     * Verdicts are returned EXACTLY as they were given at the time, not re-priced. A history screen that silently refreshes old prices shows a saving that was never actually on offer.
     * One trip and everything it found.
     */
    async getBuyerScanSession(requestParameters: GetBuyerScanSessionRequest, initOverrides?: RequestInit): Promise<GetBuyerScanSessionResponse> {
        const response = await this.getBuyerScanSessionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Keyset-paginated. Pass the `nextCursor` you were given back as `cursor`; page 500 costs the same as page 1. Cursors are opaque — do not parse them. Responses carry an ETag: send it back as If-None-Match and an unchanged page answers 304, which is free. `sort=popular` is deliberately unavailable, because a cursor into a continuously-reordering list silently skips rows.
     * Search the Crossly catalogue.
     */
    async listBuyerCatalogSearchRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BuyerOAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/buyer/catalog/search`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * Keyset-paginated. Pass the `nextCursor` you were given back as `cursor`; page 500 costs the same as page 1. Cursors are opaque — do not parse them. Responses carry an ETag: send it back as If-None-Match and an unchanged page answers 304, which is free. `sort=popular` is deliberately unavailable, because a cursor into a continuously-reordering list silently skips rows.
     * Search the Crossly catalogue.
     */
    async listBuyerCatalogSearch(initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listBuyerCatalogSearchRaw(initOverrides);
        return await response.value();
    }

    /**
     * Your scanning trips, newest first.
     */
    async listBuyerScanSessionsRaw(initOverrides?: RequestInit): Promise<runtime.ApiResponse<V1List & object>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BuyerOAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/buyer/scan/sessions`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1List &amp; objectFromJSON(jsonValue));
    }

    /**
     * Your scanning trips, newest first.
     */
    async listBuyerScanSessions(initOverrides?: RequestInit): Promise<V1List & object> {
        const response = await this.listBuyerScanSessionsRaw(initOverrides);
        return await response.value();
    }

}
