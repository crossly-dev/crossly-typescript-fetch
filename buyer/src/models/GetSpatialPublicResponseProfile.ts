/* tslint:disable */
/* eslint-disable */
/**
 * Crossly Buyer API
 * The buyer-facing Crossly API — cart, orders, wishlists, offers, cashback. A DIFFERENT principal from the seller API: authenticated by a buyer OAuth token scoped `buyer:*` and resolved by `resolveBuyer`. A seller Personal Access Token will NOT authenticate these endpoints.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { exists, mapValues } from '../runtime';
import {
    GetSpatialPublicResponseProfileContainers,
    GetSpatialPublicResponseProfileContainersFromJSON,
    GetSpatialPublicResponseProfileContainersFromJSONTyped,
    GetSpatialPublicResponseProfileContainersToJSON,
} from './GetSpatialPublicResponseProfileContainers';
import {
    GetSpatialPublicResponseProfileItemSize,
    GetSpatialPublicResponseProfileItemSizeFromJSON,
    GetSpatialPublicResponseProfileItemSizeFromJSONTyped,
    GetSpatialPublicResponseProfileItemSizeToJSON,
} from './GetSpatialPublicResponseProfileItemSize';

/**
 * 
 * @export
 * @interface GetSpatialPublicResponseProfile
 */
export interface GetSpatialPublicResponseProfile {
    /**
     * Matches `crossly_market_categories.slug`, or '*' for the fallback.
     * @type {string}
     * @memberof GetSpatialPublicResponseProfile
     */
    categorySlug: string;
    /**
     * Room title, shown in the switcher.
     * @type {string}
     * @memberof GetSpatialPublicResponseProfile
     */
    label: string;
    /**
     * One line of why this space is shaped the way it is. Surfaced in the UI.
     * @type {string}
     * @memberof GetSpatialPublicResponseProfile
     */
    tagline: string;
    /**
     * 
     * @type {string}
     * @memberof GetSpatialPublicResponseProfile
     */
    presentation: GetSpatialPublicResponseProfilePresentationEnum;
    /**
     * Other meshes items in this category may be drawn as, chosen PER ITEM.
     * 
     * One model per category is right for a card room, where every object is the
     * same object. It is wrong for a wardrobe: a rail holds tees and jeans and
     * jackets, and drawing all of them as a tee would be a worse lie than the
     * flat quad it replaced, because a wrong SHAPE reads as information.
     * 
     * The renderer picks from `[itemModel, ...itemModelVariants]` using the
     * item's title — the same heuristic `silhouetteFor` already uses to choose a
     * garment outline, and for the same reason: the title is the only signal
     * present on every item, and a wrong guess costs a slightly odd shape rather
     * than the wrong item. `itemModel` is the fallback when nothing matches.
     * 
     * Deliberately NOT fuzzy-matched against a catalog — this picks a SHAPE, not
     * an identity. See docs/IDENTIFIER-FIRST.md for where that line sits.
     * @type {Array<string>}
     * @memberof GetSpatialPublicResponseProfile
     */
    itemModelVariants?: Array<GetSpatialPublicResponseProfileItemModelVariantsEnum>;
    /**
     * 
     * @type {GetSpatialPublicResponseProfileItemSize}
     * @memberof GetSpatialPublicResponseProfile
     */
    itemSize: GetSpatialPublicResponseProfileItemSize;
    /**
     * The container ladder, OUTERMOST FIRST. A binder holds pages, a page holds
     * cards. The solver walks this to decide what to create next when the
     * current container fills up.
     * @type {Array<GetSpatialPublicResponseProfileContainers>}
     * @memberof GetSpatialPublicResponseProfile
     */
    containers: Array<GetSpatialPublicResponseProfileContainers>;
    /**
     * Default grouping, in order. Each level becomes a divider or a container.
     * @type {Array<string>}
     * @memberof GetSpatialPublicResponseProfile
     */
    defaultGroupBy: Array<GetSpatialPublicResponseProfileDefaultGroupByEnum>;
    /**
     * Default ordering inside a group.
     * @type {Array<string>}
     * @memberof GetSpatialPublicResponseProfile
     */
    defaultSortBy: Array<GetSpatialPublicResponseProfileDefaultSortByEnum>;
    /**
     * A SEPARATE profile for graded/sealed copies of the same category.
     * 
     * This is not a flourish. Nobody puts a slabbed card in a binder — it does
     * not fit and it would be vandalism. Graded cards go on a wall, raw cards go
     * in pockets, and a space that ignores that is immediately wrong to the only
     * people who would use it. Same for CGC comics.
     * @type {string}
     * @memberof GetSpatialPublicResponseProfile
     */
    gradedVariant?: string | null;
}

/**
* @export
* @enum {string}
*/
export enum GetSpatialPublicResponseProfilePresentationEnum {
    Flat = 'flat',
    Slabbed = 'slabbed',
    Boxed = 'boxed',
    Cased = 'cased',
    Spined = 'spined',
    Hanging = 'hanging',
    Standing = 'standing'
}/**
* @export
* @enum {string}
*/
export enum GetSpatialPublicResponseProfileItemModelVariantsEnum {
    Card = 'item-card',
    CardSlab = 'item-card-slab',
    Comic = 'item-comic',
    ComicSlab = 'item-comic-slab',
    Vinyl = 'item-vinyl',
    VinylDisc = 'item-vinyl-disc',
    FunkoBox = 'item-funko-box',
    LegoBox = 'item-lego-box',
    Minifig = 'item-minifig',
    GameCase = 'item-game-case',
    Book = 'item-book',
    Sneaker = 'item-sneaker',
    Boot = 'item-boot',
    Handbag = 'item-handbag',
    Watch = 'item-watch',
    Phone = 'item-phone',
    Laptop = 'item-laptop',
    Tee = 'item-tee',
    Hoodie = 'item-hoodie',
    Jacket = 'item-jacket',
    Jeans = 'item-jeans',
    Dress = 'item-dress',
    Skirt = 'item-skirt'
}/**
* @export
* @enum {string}
*/
export enum GetSpatialPublicResponseProfileDefaultGroupByEnum {
    Brand = 'brand',
    Condition = 'condition',
    Size = 'size',
    Status = 'status',
    Label = 'label',
    Title = 'title',
    Value = 'value',
    Year = 'year',
    Grade = 'grade',
    Set = 'set',
    Franchise = 'franchise',
    Game = 'game',
    Team = 'team',
    Era = 'era',
    Genre = 'genre',
    Colour = 'colour',
    Age = 'age'
}/**
* @export
* @enum {string}
*/
export enum GetSpatialPublicResponseProfileDefaultSortByEnum {
    Brand = 'brand',
    Condition = 'condition',
    Size = 'size',
    Status = 'status',
    Label = 'label',
    Title = 'title',
    Value = 'value',
    Year = 'year',
    Grade = 'grade',
    Set = 'set',
    Franchise = 'franchise',
    Game = 'game',
    Team = 'team',
    Era = 'era',
    Genre = 'genre',
    Colour = 'colour',
    Age = 'age'
}

export function GetSpatialPublicResponseProfileFromJSON(json: any): GetSpatialPublicResponseProfile {
    return GetSpatialPublicResponseProfileFromJSONTyped(json, false);
}

export function GetSpatialPublicResponseProfileFromJSONTyped(json: any, ignoreDiscriminator: boolean): GetSpatialPublicResponseProfile {
    if ((json === undefined) || (json === null)) {
        return json;
    }
    return {
        
        'categorySlug': json['categorySlug'],
        'label': json['label'],
        'tagline': json['tagline'],
        'presentation': json['presentation'],
        'itemModelVariants': !exists(json, 'itemModelVariants') ? undefined : json['itemModelVariants'],
        'itemSize': GetSpatialPublicResponseProfileItemSizeFromJSON(json['itemSize']),
        'containers': ((json['containers'] as Array<any>).map(GetSpatialPublicResponseProfileContainersFromJSON)),
        'defaultGroupBy': json['defaultGroupBy'],
        'defaultSortBy': json['defaultSortBy'],
        'gradedVariant': !exists(json, 'gradedVariant') ? undefined : json['gradedVariant'],
    };
}

export function GetSpatialPublicResponseProfileToJSON(value?: GetSpatialPublicResponseProfile | null): any {
    if (value === undefined) {
        return undefined;
    }
    if (value === null) {
        return null;
    }
    return {
        
        'categorySlug': value.categorySlug,
        'label': value.label,
        'tagline': value.tagline,
        'presentation': value.presentation,
        'itemModelVariants': value.itemModelVariants,
        'itemSize': GetSpatialPublicResponseProfileItemSizeToJSON(value.itemSize),
        'containers': ((value.containers as Array<any>).map(GetSpatialPublicResponseProfileContainersToJSON)),
        'defaultGroupBy': value.defaultGroupBy,
        'defaultSortBy': value.defaultSortBy,
        'gradedVariant': value.gradedVariant,
    };
}

